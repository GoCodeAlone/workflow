### Adversarial Review Report

**Phase:** plan
**Cycle:** 5 (unbounded per user direction)
**Artifact:** `docs/plans/2026-05-03-iac-conformance-and-replace.md` (commit a3b6988)
**Status:** FAIL

---

**Findings (Critical):**

- **[Repo-precedent conflict — load-bearing path is wrong]** §rev5 introduces ADR at `decisions/0001-providerplanner-deferred-to-first-consumer.md` (lines 61, 2273, 2279, 2282, 2648). The path matches the `recording-decisions` SKILL convention literally — but it **conflicts with the workflow repo's pre-existing ADR convention**. Verified: the workflow repo (and this worktree) already has `docs/adr/` populated with 5 ADRs using a 3-digit prefix and `# ADR NNN: <Title>` heading, `## Status` section heading: `docs/adr/001-yaml-driven-config.md`, `002-modular-framework.md`, `003-yaegi-dynamic-components.md`, `004-hybrid-ai-approach.md`, `005-field-contracts.md`. No `decisions/` directory exists at the worktree root; running `find . -type d -name decisions` returns zero matches. The plan's ADR will land at the WRONG PATH (`decisions/` will be created at workflow's repo-root, parallel to `docs/`), with WRONG NUMBERING (rev5 uses `0001` 4-digit when repo precedent is `001` 3-digit; the next free ADR number per repo precedent is `006`, not `0001`), and WRONG FORMAT (rev5 uses `**Status:** Accepted` line-style; repo precedent is `## Status\nAccepted` section-style). T9.1's `Step 0` will create `decisions/0001-...md` and the eventual repo will end up with TWO competing ADR locations — operators searching `docs/adr/` won't find this decision; tooling that scans `docs/adr/` (if any) won't see it. The cycle-4 review explicitly called this out as Specific Angle #5 ("Does the repo have an ADR convention already?") and the rev5 author created the ADR without checking. **Recommendation:** rename the file to `docs/adr/006-providerplanner-deferred-to-first-consumer.md`; rewrite the heading to `# ADR 006: ProviderPlanner Deferred to First Consumer`; convert `**Status:** Accepted` → `## Status\nAccepted`, `**Date:** 2026-05-03` → drop (repo precedent doesn't include date; if needed, mention in Context section), `**Decided by:** Jon Langevin` → drop or move to Context (repo precedent doesn't include Decision-makers); convert the three sections (`## Decision`, `## Reasoning`, `## Consequences`) to repo precedent's `## Context` / `## Decision` / `## Consequences` (Michael Nygard 3-section, not the rev5 author's 4-section variant). Sweep all 5 plan references to point at the new path.

- **[ADR ghost-writes user consent on a contested decision]** §`docs/adr/...` content (lines 2284-2308) records `Decided by: Jon Langevin` + `Status: Accepted` for dropping ProviderPlanner — but the user has not explicitly approved this. Trace the conversation: cycle-3 reviewer recommended dropping ProviderPlanner per YAGNI; rev4 dropped it; cycle-4 reviewer explicitly questioned the drop ("rev4 dropping ProviderPlanner contradicts the don't-defer mandate") AND recommended an ADR as the resolution path; rev5 then wrote the ADR with the user as decision-maker. The user's verbatim mandate was *"Don't defer any fixes, refactor if you have to, let's build these fixes the right way"* — the cycle-4 reviewer's framing is that an ADR converts "we removed it" into "load-bearing decision the next reviewer can challenge or accept". A load-bearing decision needs the actual decision-maker to sign it. The plan author is not the user. **The ADR is currently a self-recorded justification of the author's prior choice, signed in the user's name.** This is the kind of doc that, six months later, a contributor reads and assumes the user owned the call when they didn't. **Recommendation:** EITHER (a) escalate the ProviderPlanner drop as an explicit open question to the user before merging the plan (genuine consent), and only then file the ADR with the user's recorded approval, OR (b) change `Decided by:` to `Claude (autonomous-pipeline rev5; user did not explicitly approve)`, and add a Status note `Awaiting user ratification — see open question in plan §W-9` so the deferral is honest about its provenance. Option (a) is cleaner. Either way, the plan must surface this as an open question rather than treating it as a settled decision.

- **[T1.1's pre-step "verification" is a runtime check inside a writing-time plan — implementer cannot run it before they have the plan in their worktree]** §T1.1 lines 125-126 instruct implementer to "before modifying anything, run `grep -n "type ApplyResult" interfaces/iac_state.go`" — but this is a **plan-author-time** verification (the plan should already know the answer; if the assumption is wrong, the plan is wrong before any implementer touches it). Worse, the rev5 fix ALSO assumes ApplyResult lives in `interfaces/iac_state.go` for T3.0.4 (line 975) and T3.4 (line 1405) — but only T1.1 has the pre-step grep. If the assumption is wrong (e.g., ApplyResult is actually declared in `interfaces/iac_apply.go` or `interfaces/iac_provider.go`), THREE tasks across two PRs (T1.1, T3.0.4, T3.4) ship pointing at the wrong file; T3.0.4's rollback note (line 1031) explicitly couples T3.0.4 + T3.1 + T3.1.5 as a unit, so the wrong-file assumption cascades. The plan-phase fix is to verify the ApplyResult location AT PLAN TIME (during the rev5 authorship pass), not at implementation time. This was raised as Specific Angle #5 in cycle-4 review's Options ("add a pre-condition assertion to T1.1's Steps that explicitly verifies ApplyResult is in interfaces/iac_state.go") — rev5 added the assertion, but the assertion is the wrong remediation for plan-phase verification. **Recommendation:** the plan author should run the grep ONCE at plan-revision time and either (a) confirm the assumption is correct and remove the runtime pre-step from T1.1 (plan no longer hedges on a fact the author knows), or (b) discover the assumption is wrong and rewrite T1.1 + T3.0.4 + T3.4 to point at the correct file. If the worktree happens to be at a state where this can't be verified (clean worktree, file may have moved on main), say so explicitly in the plan and surface as an open question. The runtime-pre-step is an evasion of the plan-phase verification responsibility.

---

**Findings (Important):**

- **[Cross-package contract — T1.5 documents the sentinel honoring inside its own task body but the actual `ComputeDrift` SOURCE FILE is created in T1.5 itself, while the sentinel is created in T1.2 — meaning T1.5's commit ALONE doesn't compile the new ComputeDrift unless T1.2 has already merged]** §T1.5 Step 3 (line 588) says ComputeDrift is added in `compute_drift.go`. T1.2 (lines 277-353) introduces `PreservedFingerprint` constant + `NewTolerantEnvProvider`. T1.5's commit (line 600) lists `iac/inputsnapshot/compute_drift.go` for the first time. The dependency direction is fine (T1.2 → T1.5 in commit order within the same PR W-1), but the PLAN doesn't make the cross-task contract testable. T1.2's tests (lines 359-377) cover `NewTolerantEnvProvider` + `Compute`'s sentinel pass-through, but NOT `ComputeDrift`'s sentinel honoring (because the file doesn't exist yet at T1.2 commit time). T1.5's tests (lines 524-573) cover the persisted-`--plan` path's `ErrEnvVarChanged` propagation but NOT the sentinel-honoring branch (the persisted path uses `os.LookupEnv`, not `NewTolerantEnvProvider` — sentinel never appears in that path's apply-snap; the test never exercises `applyFP == PreservedFingerprint`). **The sentinel-honoring branch in `ComputeDrift` is FIRST exercised by T3.1.5's test in W-3a — across a PR boundary.** If W-1 lands and W-3a doesn't, the sentinel-honoring branch is untested in main. **Recommendation:** add a unit test to T1.5's commit that exercises `ComputeDrift` with a sentinel-bearing applySnap directly (table-driven test in `compute_drift_test.go` — no postcondition or env-provider involvement; just the pure function with the sentinel as input). One test, ~15 lines. Closes the cross-PR test gap and makes ComputeDrift's contract self-contained within W-1.

- **[T9.1's "Step 0 (rev5)" creates the ADR but the workflow file's commit message is `ci(iac): cross-plugin build gate verifies AWS/GCP/Azure compile against this PR's workflow head` (line 2361) — single-commit task with a `decision(iac):` commit (line 2310) PLUS the workflow file commit. Two commits in one task; commit ordering is unspecified]** §T9.1 has `Step 0` (line 2282 — ADR creation) with commit message `decision(iac): record ProviderPlanner deferred-to-first-consumer rationale (rev5)` (line 2310), THEN `Step 1` through `Step 5` (lines 2312-2362) with commit message `ci(iac): cross-plugin build gate verifies...` (line 2361). The plan implies these are two distinct commits, but the commit ordering is unspecified — does the ADR commit go first (decision then implementation) or last (implementation then decision)? If the ADR is the rationale for the work, it should land first. If the plan implies they're squashed into one PR commit per the W-9 PR-Grouping table (line 95 lists "T9.1" as the only task in PR 10), they should be one commit with both parts. The Task Conventions (line 107) say "Commit messages follow conventional commits" — singular. **Recommendation:** make the commit boundary explicit. Either (a) consolidate Step 0 + Steps 1-5 into a single commit `ci(iac): cross-plugin build gate + ADR for ProviderPlanner deferral`, or (b) explicitly enumerate the 2-commit ordering: "Commit 1 (Step 0): the ADR. Commit 2 (Steps 1-5): the workflow + tests." The PR-Grouping table footnote should match. Today the plan implies two commits but doesn't say which goes first.

- **[Per-PR drafting graph still allows T3.0.4 + T3.1 + T3.1.5 + T3.4 to all touch `interfaces/iac_state.go` within W-3a — 4 commits modify the same file in serial]** §W-3a has 8 tasks (T3.0, T3.0.4, T3.1, T3.1.5, T3.2, T3.3, T3.4, T3.5). T3.0.4 modifies `interfaces/iac_state.go` (line 975); T3.4 modifies `interfaces/iac_state.go` again (line 1405). Per the in-PR commit order (line 1147: T3.0 → T3.0.4 → T3.1 → T3.1.5 → T3.2 → T3.3 → T3.4 → T3.5), this is fine for forward implementation. But cycle-4 reviewer's Specific Angle #4 raised the concern: ApplyResult split across 3 separate commits modifies the same file — "Strict serial merge order is required; rebase chain may be fragile if W-3a is force-pushed during review." rev5 reduced this from 3 commits to 2 commits (T3.0.4 ships 2 fields; T3.4 ships ReplaceIDMap), which is better, but the concern remains for the rebase chain across review rounds. The plan does not document the rebase strategy if W-3a force-pushes during review (which it WILL, per the rev1-rev5 history of this very plan). **Recommendation:** add a sub-section to W-3a's intro: "**Rebase strategy:** if W-3a force-pushes during review, the per-task commit chain (T3.0.4 + T3.4 both touch `interfaces/iac_state.go`) requires `git rebase --interactive` with `pick` for both commits, NOT `squash` — the per-task verification (each task's Step 4) depends on per-commit boundaries. Reviewers checking individual commits via `gh pr view <N> --json commits` see the field-additions land in their declaring tasks." Cycle-4 reviewer's Option 1 was specifically "Defer T1.1's ApplyResult field additions to a single commit at the top of W-3a" — rev5 split them across T3.0.4 (2 fields) and T3.4 (1 field). Is this split worth its complexity, or should all 3 fields land in T3.0.4 for cleaner Git history (only ONE commit modifies `interfaces/iac_state.go` in W-3a, T3.4 just reads ReplaceIDMap)? Both options are defensible; the plan should explain why the split was preferred (the rev5 changelog line 60 just says "moved out of W-1 into W-3a" — doesn't address the 1-vs-2-commit question).

- **[`PreservedFingerprint` sentinel value `"__plan_time_preserved__"` is publicly exposed but no protection against a future caller injecting it intentionally]** §T1.2 (line 334) declares the sentinel as an exported constant. The cross-function contract says: `Compute` passes the sentinel through unhashed; `ComputeDrift` skips drift detection on sentinel-bearing keys. But: a third caller of `inputsnapshot.Compute` that uses a custom env-provider (not `OSEnvProvider`, not `NewTolerantEnvProvider`) could intentionally inject the sentinel value, suppressing drift detection on whatever key they pleased. This is documented as a "feature" (the sentinel preserves the fingerprint), but it's also a back-door that any code path with control over the env-provider can use to defeat plan-stale detection. The safer design is to make the sentinel UN-CALLABLE from outside the package: keep the sentinel constant unexported (`preservedFingerprint`), keep `NewTolerantEnvProvider` exported (it's the only sanctioned way to inject the sentinel), and have `Compute` check `if val == preservedFingerprint` (in-package access). External callers MUST use `NewTolerantEnvProvider` to opt into the tolerance contract. **Recommendation:** unexport `PreservedFingerprint` to `preservedFingerprint`; keep `NewTolerantEnvProvider` exported as the sanctioned access path; add a doc comment to `NewTolerantEnvProvider` explaining "only sanctioned way to inject the preservation sentinel; direct callers of Compute cannot bypass drift detection." Reduces the API surface area + closes the bypass channel. Test changes: T1.2's `TestCompute_PreservesSentinel` (line 369) becomes a same-package test that imports the unexported constant; no behavior change.

- **[The "rev6 (if needed)" line in changelog (line 71) violates conventional changelog style + creates an open commit pre-condition]** §line 71: `- rev6 (if needed) — further revisions per cycle-5 review.` This pre-empts the cycle-5 review (this report) by reserving a slot for rev6 in the changelog before the review has produced findings. If cycle-5 PASSes, the rev6 line is dead text. If cycle-5 FAILs (as this report does), the rev6 line is correct but the review's findings should drive the rev6 content, not a pre-emptive placeholder. **Recommendation:** remove line 71. If a rev6 is needed, the rev6 author writes the rev6 entry. Pre-allocating the changelog slot before the review runs is the kind of detail that, six months later, a reader sees and wonders "did rev5 already know it was going to fail review?" Don't ghost-write changelog entries.

- **[Smoke gate cost claim "$0.005/PR" is asserted twice (lines 2075 + 2560) but the underlying math has not been re-verified since rev2]** §T7.13 line 2075: `~$0.005 (Droplet s-1vcpu-512mb-10gb @ $4/mo / 30 days / 24 hrs / 60 min ≈ $0.00009/min × ~5 min lifetime)`. Math: $4/mo / 30 / 24 / 60 = $0.0000926/min; × 5 min = $0.000463 = ~$0.0005 (HALF a tenth of a cent), NOT $0.005. The plan over-states the cost by 10x (a $0.0005 cost is being reported as $0.005). This was likely a unit error in the original calculation that has propagated through 4 cycles. The same line-2560 reference says `~$0.005/PR × ~600 PRs/mo ≈ $3/mo` — if the actual cost is $0.0005/PR, the monthly is $0.30/mo, well below the soft-alert threshold ($15/mo). DO Droplet pricing as of 2026-05-03 should be re-verified; if a Droplet starts at $4/mo (~$0.0056/hr), 5 minutes is $0.00046. **Recommendation:** sweep all "$0.005/PR" references to the correct value ($0.0005/PR or "<$0.001/PR — sub-tenth-cent"); recompute the monthly burn estimate; verify the soft-alert/hard-stop thresholds make sense at the corrected scale. Also: per the workspace memory feedback `feedback_check_versions_actively`, re-verify the Droplet's current per-month price before the PR ships — DO has changed pricing tiers historically.

---

**Findings (Minor):**

- **[Documentation]** §rev5 changelog (lines 51-71) is now grouped Critical/Important/Minor per the cycle-4 minor finding — clean. But the rev4 changelog block (lines 37-50) is NOT grouped (still flat 14 bullets). If the grouping convention is being introduced, apply retroactively to rev4 OR explicitly note "rev4 retains original flat layout; grouping starts at rev5." Inconsistency invites future readers to wonder.

- **[Documentation]** §line 71 `- rev6 (if needed) — further revisions per cycle-5 review.` See Important finding above; remove.

- **[Documentation]** §rev5 fix line 70 says `All T9.3 references swept to T9.1 (rev4 renumber)`, but `grep T9.3` returns 7 hits in the file (lines 33, 40, 42, 49, 50, 70, 2271, 2635). All 7 are in CHANGELOG/REVISION-HISTORY/NOTE prose (intentional history references), not in active task references. The sweep claim is technically true (no active task currently lists T9.3 as a target); the historical references are appropriate. But the changelog claim ("All T9.3 references swept") is misleading — it should be "All ACTIVE T9.3 references swept; historical changelog entries retained for revision-history continuity." Minor wording.

- **[Documentation]** §T3.4's commit message (line 1469) is `feat(iac): doReplace declares + populates ApplyResult.ReplaceIDMap (delete-then-create with ProviderID propagation)` — wraps to ~120 chars, beyond conventional commits' 72-char first-line guideline. Consider trimming to `feat(iac): doReplace declares + populates ApplyResult.ReplaceIDMap` and putting the parenthetical in the commit body.

- **[Documentation]** §rev5 changelog line 56 says `'replaceIDMap' → 'ReplaceIDMap' swept everywhere except function-parameter names (where lowercase is conventional). Verified via grep "\\breplaceIDMap\\b" docs/plans/...`. Verified: `grep "\breplaceIDMap\b"` returns 3 hits — line 57 (the changelog itself referring to the lowercase form), line 1901 (the T5.1 function parameter), line 1904 (the T5.1 prose describing the parameter). All 3 are intentional. Sweep is clean. (No finding; verification.)

- **[Documentation]** §T7.13 cache-step inline comment (lines 2091-2094) is a great addition per cycle-4 Important — but the comment doesn't actually document that the file `/tmp/budget-result.json` is the path being written. An implementer reading just the YAML sees `path: /tmp/budget-result.json` (line 2097) but the comment says "the action records the path's contents at job-end" — should explicitly say "the path is `/tmp/budget-result.json` per the with: block above." One-line addition; closes a small reading-flow gap.

---

**Bug-class scan transcript:**

| Class | Result | Note |
|---|---|---|
| Unstated assumptions | Finding | T1.1 + T3.0.4 + T3.4 all assume `ApplyResult` lives in `interfaces/iac_state.go` — only T1.1 has a pre-step verification, and that verification is at runtime not plan-time (Critical 3); ADR location assumed to be `decisions/` per skill convention without checking repo precedent (Critical 1). |
| Repo-precedent conflicts | Finding | ADR convention: repo uses `docs/adr/NNN-...md` 3-digit + `# ADR NNN: Title\n## Status` format; rev5 uses `decisions/NNNN-...md` 4-digit + `# NNNN — Title\n**Status:** Accepted` format (Critical 1). |
| YAGNI violations | Clean | rev5 doesn't introduce new speculative interfaces. ProviderPlanner stays dropped per cycle-3. |
| Missing failure modes | Finding | ComputeDrift's sentinel-honoring branch is first tested in T3.1.5 (W-3a) — across a PR boundary; if W-1 lands and W-3a doesn't, the branch is untested in main (Important). |
| Security / privacy | Finding | `PreservedFingerprint` exported; any external caller of `Compute` with a custom env-provider can inject the sentinel and suppress drift detection (Important). |
| Rollback story | Clean | T3.0.4 + T3.4 + T3.1.5 rollback notes correctly couple their removal as a unit. |
| Simpler alternative not considered | Finding | All 3 ApplyResult fields could ship in T3.0.4 (single `interfaces/iac_state.go` modification in W-3a) instead of T3.0.4 (2 fields) + T3.4 (1 field) — rev5 chose the split without explaining why (Important). |
| User-intent drift | Finding | ADR ghost-writes user as `Decided by` for a decision the user has not explicitly approved (Critical 2). |
| Over-decomposition | Clean | W-3a has 8 tasks, in-spec for the rev3 split. T3.0.4 is a tiny but well-scoped task (2 field additions + 2 tests). |
| Verification-class mismatch | Finding | T1.1 uses runtime grep as plan-author-time verification (Critical 3). |
| Hidden serial dependencies | Clean | T3.0.4 → T3.1 → T3.1.5 ordering documented in W-3a sequencing note. |
| Missing rollback wiring | Clean | Per-task Rollbacks present including the new T3.0.4. |
| Cost claims math | Finding | $0.005/PR claim is 10x over actual ($0.0005/PR) — propagated through 4 cycles uncorrected (Important). |

---

**Cross-checks against rev5-claimed fixes:**

| rev5 claim | Verified? | Note |
|---|---|---|
| `envProviderTolerantOfUnset` factory invoked (not passed by name) | PASS | T3.1.5 Step 3 line 1219 calls `inputsnapshot.NewTolerantEnvProvider(plan.InputSnapshot)` returning the closure. |
| `PreservedFingerprint` constant declared in T1.2's `iac/inputsnapshot/snapshot.go` | PASS | Line 334. |
| ComputeDrift sentinel-honoring `if applyFP == PreservedFingerprint { continue }` | PASS | Line 1278. |
| Cross-function contract documented at all 3 sites | PASS (3 sites named line 1292-1294); but the documentation citing 3 sites refers to `compute_drift.go::ComputeDrift body comment` — verified via the code block at line 1278-1280 has a `// Sentinel — sub-action cleanup unset; not real drift.` comment. |
| T1.1's Files+Steps reflect ONLY IaCPlan + PlanAction + DriftEntry | PASS | W-1 grep returns zero ApplyResult field references (only `type ApplyResult` in the pre-step verification). |
| T3.0.4 exists as proper task with Files/Steps/Rollback | PASS | Lines 972-1031 have Files (2), Steps (5), Rollback. NO separate "Verification" section header (consistent with T1.1). |
| T3.4's Files lists `interfaces/iac_state.go` + the ReplaceIDMap declaration step | PASS | Line 1405 + Step 2 at line 1440-1448. |
| Zero `T3.0.5` references in active prose | PASS for ACTIVE prose | 7 references remain in changelog/note prose (intentional history); no active task header uses T3.0.5. |
| PR-Grouping table row for W-3a lists T3.0, T3.0.4, T3.1, T3.1.5, T3.2 – T3.5 | PASS | Line 88. |
| `replaceIDMap` → `ReplaceIDMap` swept (param names exempt) | PASS | 3 lowercase residuals: line 57 (changelog), line 1901 (T5.1 param), line 1904 (T5.1 prose) — all intentional. |
| `actions/github-script` SHA-pinned + paginated | PASS — verified SHA `60a0d83039c74a4aee543508d2ffcb1c3799cdea` matches `actions/github-script v7.0.1` via `gh api repos/actions/github-script/git/refs/tags/v7.0.1` (genuine SHA, not hallucinated). Pagination via `github.paginate(github.rest.issues.listComments, ...)` line 2415. |
| T7.13 discrete `id: hour` step BEFORE cache step | PASS | Lines 2087-2089 = hour-output step; lines 2089-2097 = cache step referencing `steps.hour.outputs.value`. |
| ApplyResult fields moved to W-3a | PASS for content; **FAIL for ADR provenance** (Critical 2). |
| ADR `decisions/0001-...md` added with cycle-4 reasoning | EXISTS (in plan body at lines 2284-2308); but path + format + decision-attribution all wrong (Critical 1 + Critical 2). |
| T7.14 operator runbook note | PASS | Line 2160. |
| T7.13 inline comment about actions/cache@v4 post-step | PASS | Lines 2091-2094. |
| Task count recomputed (71) | PASS arithmetic; per scope manifest line 18: `rev3=72; +T3.1.5; +T3.0.4; −T9.1 −T9.2; = 71`. |
| T9.3 → T9.1 references swept | PASS for active references; 7 historical residuals appropriate. |

---

**Options the author may not have considered:**

1. **File the ADR at `docs/adr/006-providerplanner-deferred-to-first-consumer.md`** with repo-native format. Drop the rev5 author's 4-section variant for the Nygard-3 already in use. Verify by `ls docs/adr/` showing 6 entries lexicographically sorted.

2. **Surface ProviderPlanner-deferral as an open question to the user** before merging. If the user explicitly approves, file the ADR with `Decided by: jon@langevin.me` and the user's recorded approval message in the Context section. If the user rejects, the deferral is reversed and W-9 ships with ProviderPlanner. Either path is a real decision; rev5's ghost-written ADR is neither.

3. **Run the `grep -n "type ApplyResult"` ONCE at plan-revision time** (now, in cycle 5 or rev6) and either confirm or correct the file path. Remove the runtime pre-step from T1.1 once verified. This is the plan-author's responsibility, not the implementer's.

4. **Consolidate ApplyResult field additions into T3.0.4** (all 3 fields: InitialInputSnapshot, InputDriftReport, ReplaceIDMap). T3.4 then just reads ReplaceIDMap (already populated by doReplace into the field declared by T3.0.4). Single commit modifies `interfaces/iac_state.go` in W-3a. Cleaner Git history; cleaner rebase story; matches cycle-4 reviewer's Option 1 verbatim.

5. **Unexport `PreservedFingerprint`** and make `NewTolerantEnvProvider` the only sanctioned injector. Closes the third-party-caller bypass channel.

6. **Add a `compute_drift_test.go` unit test for the sentinel-honoring branch in T1.5.** Closes the cross-PR test gap (sentinel branch currently first-tested in W-3a's T3.1.5).

7. **Re-verify the smoke-gate per-PR cost** ($0.0005 not $0.005) and update the soft-alert/hard-stop thresholds + monthly-burn estimates accordingly.

8. **Drop the pre-emptive `rev6 (if needed)` changelog line.** Let cycle-5's review findings drive whether rev6 happens.

9. **Make the T9.1 commit boundary explicit** (single commit OR two commits with documented ordering). Today the plan implies two commits without specifying order.

---

**Verdict reasoning:**

Three Critical findings: (1) ADR location + numbering + format violates the workflow repo's pre-existing ADR convention at `docs/adr/NNN-...md` (the plan author followed the `recording-decisions` skill convention literally without checking repo precedent — verified `docs/adr/` exists with 5 ADRs in 3-digit format; `decisions/` does not exist); (2) ADR ghost-writes the user as `Decided by` for a contested decision the user has not explicitly approved (cycle-3 reviewer recommended dropping ProviderPlanner; cycle-4 reviewer questioned the drop; rev5 records the drop as a user-approved decision with no explicit user consent recorded); (3) T1.1's "pre-step verification" is a runtime grep that defers plan-author-time fact-checking to implementer-time — the same assumption (ApplyResult lives in `interfaces/iac_state.go`) is shared across T1.1 + T3.0.4 + T3.4 in two PRs, so the assumption MUST be verified at plan-revision time, not at implementation time. Plus six Important findings (ComputeDrift sentinel-honoring branch first tested across a PR boundary; T9.1 commit boundary unspecified; W-3a rebase strategy undocumented for `interfaces/iac_state.go` modifications across 2 commits; PreservedFingerprint exported with no protection against external bypass; pre-emptive rev6 changelog entry; smoke-gate cost claim is 10x over reality). Plus six Minor findings (changelog grouping inconsistency, residual T9.3 history wording, T3.4 commit message length, etc.). Per the prompt's PASS criteria (zero Critical AND every Important addressed): **FAIL**. The rev5 changes correctly addressed the cycle-4 surface (sentinel design, T3.0.4 introduction, T3.0.5→T3.1.5 rename, ApplyResult split, GitHub Action SHA pin verified-real, pagination, cache step ordering, operator runbook note) but introduced THREE new defects directly attributable to the rev5 changes themselves: (a) the ADR's rev5 author wrote it from skill-convention defaults without checking repo precedent — a form of cycle-1's critique that "rev N should check rev N-1's references against repo state" extended to ADR placement; (b) the ADR's `Decided by` line claims user authority for a decision rev3-rev5 made autonomously — exactly the kind of ghost-attribution that the workspace memory entry `feedback_proper_fixes_over_workarounds.md` warns against; (c) the rev5 author added "pre-step verification" as a remediation for cycle-4's Option 5, but the remediation is verification-class-mismatched (runtime check for plan-author fact). Recommended next step (rev6): apply Options 1, 2, 3, 4, 5, 6, 7 from the "Options the author may not have considered" section. Most critical: file the ADR at `docs/adr/006-...md` with repo-native format + escalate the user-consent question explicitly. Second-most: consolidate ApplyResult field declarations into T3.0.4 (eliminates the rebase-fragility concern + eliminates the redundant `interfaces/iac_state.go` Modify in T3.4). Third: run the ApplyResult-location grep at plan-revision time + remove the runtime pre-step.
