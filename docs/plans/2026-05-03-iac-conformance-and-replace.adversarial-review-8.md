### Adversarial Review Report

**Phase:** plan
**Cycle:** 8 (unbounded per user direction)
**Artifact:** `docs/plans/2026-05-03-iac-conformance-and-replace.md` (commit 9ed8afa)
**Status:** FAIL

---

**Findings (Critical):**

- **[Open Questions § "Default if no user input" makes a factually false dependency claim — C-1 is downstream of P-DO, NOT W-9; the rev8 fix invents a phantom block on C-1]** §Line 168: "W-9 + (downstream) C-1 wait at the gate." Verified against the dependency graph at line 2822: `W-1 → W-2 → W-3a → W-3b → W-4 → W-5 → W-6 → W-7 → W-8 → P-DO → C-1`. The graph terminates at C-1; W-9 is on a parallel branch (line 2823-2824: "W-9 (CI-only; can merge anytime after W-3b; not on P-DO critical path)"). Line 2828 explicitly says: "W-9 … is NOT on P-DO's critical path." Line 2854 confirms: "P-DO can draft after W-7 + W-8 merge" — no W-9 dependency. Line 2855: "C-1 must merge after P-DO ships v0.10.0." Therefore C-1 has zero merge dependency on W-9; the Open Questions claim that C-1 "waits at the gate" along with W-9 is contradicted by 4 separate cross-references in the same plan. The rev8 author appears to have manufactured a downstream-block to make the halt-and-surface mechanism feel more consequential, but the plan's own sequencing graph says C-1 ships independently of the user-ratification ask. **Recommendation:** delete the "(downstream) C-1" parenthetical from line 168 — only W-9 itself waits at the gate. If the author wanted C-1 to also wait, the dependency graph at line 2822-2824 needs updating with a `W-9 → C-1` edge (and a justification — there isn't one, since C-1's behavior depends on the v2 plugin shipped by P-DO, not on the cross-plugin-build CI gate). The cleaner fix is to drop the parenthetical and accept that only W-9 itself blocks; C-1 ships per its own graph.

- **[rev8 changelog claim "ADR Context section deduplicated (rev7 left two competing `## Context` blocks)" is fabricated — rev7 had ONE Context block, not two]** §Line 119 (rev8 Minor changelog): "ADR Context section deduplicated (rev7 left two competing `## Context` blocks; rev8 keeps the one with the Provenance line)." Verified against `git show 791494a:docs/plans/2026-05-03-iac-conformance-and-replace.md | grep -nE "## Context"`: rev7 (commit 791494a) had a single `## Context` heading at line 2406. There were not "two competing blocks." The rev8 changelog invents a defect that did not exist, then claims to fix it. Pattern observation: this is the same bug-class the cycle-7 reviewer caught with the "preserves tooling compatibility" claim — a changelog entry that ghost-attributes a fix to a non-existent prior-revision defect. Cycle-6 caught self-contradicting changelog claims; cycle-7 caught false-positive tooling claims; cycle-8 catches fabricated-defect claims. **Recommendation:** delete line 119 entirely OR rewrite to describe what rev8 actually changed (if anything was changed in the Context section beyond adding the Provenance line, document THAT specifically; if nothing was changed, drop the claim).

- **[Scope Manifest task-count arithmetic is internally inconsistent — claim is 71, computation yields 72, actual headings count to 72]** §Line 18: "Tasks: 71 (counting T3.6a–f as 6 separate tasks and T7.2–T7.12 as 11 scenario tasks; rev5 net: rev3=72; +T3.1.5 (formerly T3.0.5; T1.5 split into W-1 portion + W-3a portion); +T3.0.4 (NEW field declarations); −T9.1 + −T9.2 (rev4 W-9 ProviderPlanner drop); = 71)". The arithmetic shown: 72 + 1 (T3.1.5) + 1 (T3.0.4) - 2 (T9.1 + T9.2) = **72**, NOT 71. Verified by counting `grep -nE "^### Task T"` = 62 visible headings, of which T7.2-T7.12 collapses 11 tasks under 1 heading (+10), giving 72 actual tasks. The cycle-7 reviewer caught the PR-count contradiction (12 vs 11 at line 2754) and the rev8 author fixed THAT specific instance; the rev8 author also added a Task Conventions sweep-discipline line (line 179) that explicitly says "For numerical claims (PR counts, task counts), cross-check against the Scope Manifest table." But the SAME numerical-sweep discipline that catches the PR-count fix would catch this task-count error, and it didn't. The rev8 sweep-discipline addition is a process-fix that fails on its first numerical claim. **Recommendation:** either (a) recompute the Scope Manifest count to 72 (and update Final Verification's count if it ever appears as a "tasks" number), or (b) identify which task was actually removed to make the count 71. The rev5 changelog at line 70 has the same error: "rev5 adds T3.0.4 = +1; net: 72-1=71" — the math `72-1=71` is correct as arithmetic but the inputs are wrong (the actual progression is rev3=72, +T3.1.5, -T9.1, -T9.2, +T3.0.4 = 72 + 1 - 2 + 1 = 72, NOT 71). The "71" claim has propagated through rev5/rev6/rev7/rev8 unchallenged.

---

**Findings (Important):**

- **[rev7 changelog (line 101) still claims "honest provenance lives in the body via `Decided by:` + Context section" but the rev8 ADR has NO `Decided by:` field — only a `Provenance:` line]** §Line 101: "honest provenance lives in the body via `Decided by:` + Context section." But the rev8 ADR body (line 2471) does NOT have a separate `Decided by:` field; it has a single `**Provenance:**` line that contains the phrase "Decided by Claude" inline. The rev7 changelog claim is now stale — it describes a structural form (`Decided by:` field) that rev8 collapsed into the `Provenance:` line. The rev8 changelog at line 109 correctly describes the new structure ("Provenance + ratification cross-reference moved to a `**Provenance:**` line in the Context section"), but the rev7 changelog wasn't updated to reflect that its claim no longer matches the ADR body. **Recommendation:** either (a) edit line 101's wording to past-tense + add a parenthetical noting rev8 collapsed the field ("(rev8 collapsed `Decided by:` field into the `Provenance:` line; see rev8 changelog)"), or (b) leave rev7's text as historical truth-at-rev7 and accept the time-shifted inconsistency; the cleaner path is (a).

- **[Open Questions halt-and-surface mechanism is unspecified — plan does NOT say HOW the autonomous-pipeline halts or HOW the PR comment is posted]** §Line 168: "the autonomous-pipeline HALTS at W-9's pre-merge gate and surfaces the ratification ask via PR comment; W-9 does NOT merge until the user replies." The plan does not specify: (a) WHAT mechanism implements the halt (a CI workflow that fails the pre-merge gate? an executing-plans skill behavior? operator-driven manual hold?); (b) WHO posts the PR comment (a CI workflow? `gh pr comment` from the implementer? the team-lead?); (c) WHAT the PR comment looks like (template? exact text?); (d) WHAT counts as a valid user reply ("ratified" / "override" — but where? PR comment? checkbox click? GitHub Issue?); (e) WHAT happens if the user replies "ratified" — does the implementer manually mark the checkbox + push? Does CI re-run? The mechanism is hand-waved. The cycle-7 reviewer's Critical 3 was about default policy alignment; rev8 fixed the policy direction (halt vs. silent-ratify) but did not specify the operational mechanism. An autonomous pipeline cannot "HALT at the pre-merge gate" without a concrete enforcement surface. **Recommendation:** add a "Mechanism" sub-section under the Default block specifying: (a) "before merging W-9, the executing-plans agent reads `## Open Questions` and checks for an updated checkbox in the active PR description or in this plan file; if neither checkbox is checked, the agent posts `gh pr comment` with the ratification ask + halts"; (b) the exact PR comment template; (c) what user actions resume the pipeline. Otherwise the default is operationally meaningless.

- **[Open Questions checkbox UX has no specified click-surface — checkboxes render in GitHub PR view but the plan file lives at `docs/plans/...md`, not in a PR description]** §Lines 165-166: `- [ ] Ratify the deferral` / `- [ ] Override`. Markdown checkboxes are interactive in GitHub Issues / PR descriptions / PR comments — but NOT in committed `.md` files when viewed in the GitHub file viewer (the boxes render as visual, not clickable). The plan file is at `docs/plans/2026-05-03-iac-conformance-and-replace.md` — a committed file. The user clicking the box on the file view does nothing; clicking it in a PR's "Files changed" diff view does nothing. The checkbox-as-surface only works if the same content appears in a PR description body or PR comment. The rev8 fix is theatrical: it provides visual checkboxes without a click-actuated answering surface. **Recommendation:** either (a) accept that the checkboxes are visual-only signals + add a sentence like "User ratifies by replying to the W-9 PR with `ratified` or `override`; checkboxes are visual signaling only," or (b) require the executing-plans agent to copy the Open Questions block into the W-9 PR description (where checkboxes ARE click-actuated) and watch for the checkbox state change. Without one of these, the checkbox UX is misleading.

- **[T1.5 Files-list entry for `compute_drift.go` declares the constant ALSO appears in T3.1.5's body — risk of dual-source-of-truth drift]** §Line 597: "`iac/inputsnapshot/compute_drift.go` (rev8 — added per cycle-7 Critical 2; declares `unsetFingerprintPlaceholder` constant + `ComputeDrift` function; honors `preservedFingerprint` sentinel from T1.2)". Line 668-708: T1.5 Step 3.5 has the canonical code block. Line 708: "T3.1.5's body re-states this code block for reading-flow context but the canonical declaration is here." Verified by reading T3.1.5 around line 1324-1505 (not fully read in this review — rely on the rev7 reviewer's observation that T3.1.5 has an embedded code block at the equivalent position). The "re-states for reading-flow context" pattern means the same constant declaration appears literally in TWO places in the plan — T1.5 (canonical) and T3.1.5 (re-statement). If a future revision updates T1.5's constant value (say, `"(unset)"` → `"<not-set>"`) and forgets to update T3.1.5's re-statement, the plan internally contradicts itself. The rev8 sweep-discipline at line 179 catches "Files-section-vs-code-block" mismatches, but it doesn't catch "code-block-in-T1.5 vs code-block-in-T3.1.5" drift specifically. **Recommendation:** in T3.1.5's body, replace the literal code-block re-statement with a one-line cross-reference: "`ComputeDrift` is declared in T1.5 (`iac/inputsnapshot/compute_drift.go`); see Step 3 §3 of T1.5 for the function body." Single source of truth. Eliminates dual-update risk.

---

**Findings (Minor):**

- **[Five "this revision" leftover markers (lines 23, 53, 67, 73, 91) — only rev8 line 107 is currently "this revision"]** §`grep -n "this revision"` returns 6 hits: line 23 (rev3), 53 (rev5), 67 (rev5 inner), 73 (rev6), 91 (rev7), 107 (rev8). Of these, only line 107 is currently "this revision." The others should read "rev3 — addresses cycle-2 findings" / "rev5 — addresses cycle-4 findings" etc. (drop the "(this revision)" parenthetical when it ages out). The rev2/rev4 entries (lines 22, 37) correctly DON'T say "this revision" — those entries were updated when their revision was no longer active. The rev3/rev5/rev6/rev7 entries didn't get the same treatment. **Recommendation:** sweep `grep -n "this revision"` and edit out all 5 stale entries (keep only line 107 for rev8). Add a Task Conventions or implicit rule: when a new revision lands, the prior-revision changelog block has its "(this revision)" parenthetical removed. The rev8 sweep-discipline (line 179) doesn't explicitly catch this annotation rot.

- **[Open Questions intro section number `### 1.` was claimed dropped (rev8 changelog Minor) but was already absent in rev7 — historical fix, not rev8]** §Line 155: `### ProviderPlanner deferral — ratify or override` — no number. Verified against `git show 791494a:...md | grep "### ProviderPlanner"` = `### ProviderPlanner deferral — ratify or override` (same — no number in rev7 either). The rev8 changelog Minor item at line 118 ("Open Questions section number `### 1.` dropped (single-item section doesn't need numbering)") describes a fix that wasn't needed — rev7 already had no number. The cycle-7 reviewer's Minor finding said "Line 142: `### 1. ProviderPlanner deferral`" — but verifying against rev7 (commit 791494a), line 142 was actually `### ProviderPlanner deferral — ratify or override` without a number. The cycle-7 reviewer's Minor was incorrect; rev8 then "fixed" the non-existent issue. Same false-defect pattern as Critical 2 above. **Recommendation:** drop line 118 from the rev8 changelog OR document that the "1." was never there + the rev7-reviewer-finding was a false-positive.

- **[`rev3 changelog still references "T3.0.5"` — historical reference is OK in the changelog, but the rev3 wording reads as if T3.0.5 is the current name]** §Line 24-36 (rev3 changelog) — multiple bullets. Specifically rev4 changelog line 39 says "T1.5 split into T1.5 (W-1) + T3.0.5 (W-3a)". Within historical changelog context this IS accurate (T3.0.5 was the rev4 name; rev5 renamed to T3.1.5). The rev5 changelog line 57 then says "T3.0.5 renamed to T3.1.5." A reader scanning top-down through changelog encounters T3.0.5 first (rev4) without knowing it's historical. **Recommendation:** none required; flag as a minor convention question.

---

**Bug-class scan transcript:**

| Class | Result | Note |
|---|---|---|
| Unstated assumptions | Finding | Open Questions halt mechanism unspecified — Important. |
| Repo-precedent conflicts | Clean | rev8 ADR Status now bare `Accepted` matching all 5 prior ADRs. |
| YAGNI violations | Clean | ProviderPlanner remains dropped. |
| Missing failure modes | Finding | Halt-and-surface mechanism has no enforcement surface — Important. |
| Security / privacy | Clean | No new attack surface introduced by rev8. |
| Rollback story | Clean | Per-task rollbacks still present; T1.5 Files-section addition doesn't change rollback semantics. |
| Simpler alternative not considered | Finding | T3.1.5 dual-code-block could be a single cross-reference — Important. |
| User-intent drift | Clean | rev8 corrected the silent-ratification default to halt-and-surface. |
| Over-decomposition | Clean | Task count steady (claimed 71; actual 72 — Critical 3). |
| Verification-class mismatch | Clean | Plan-author-time verification preserved. |
| Hidden serial dependencies | Finding | Open Questions claims C-1 waits with W-9, but graph contradicts — Critical 1. |
| Missing rollback wiring | Clean | All runtime-affecting tasks have Rollback notes. |
| Cost claims math | Clean | Smoke-gate cost remains $0.0005/PR (rev6 correction). |
| Internal consistency / changelog truthfulness | Finding | Fabricated "two competing Context blocks" claim (Critical 2); stale rev7 changelog `Decided by:` reference (Important); false `### 1.` Minor; task-count 71 vs 72 (Critical 3). |
| Dangling cross-references | Finding | Open Questions C-1 reference dangles against the dependency graph — Critical 1. |
| Sweep completeness | Finding | "this revision" sweep missed (5 leftover markers); task-count sweep missed (Critical 3); rev7 changelog `Decided by:` not updated to match rev8 ADR — Important + Minor. |
| Test brittleness | Clean | rev8 test changes are test-code-only; no brittleness risk introduced. |
| Default-policy alignment | Clean | rev8 default now aligns with "don't defer fixes" mandate. |
| Operational mechanism specification | Finding | Halt mechanism + checkbox UX have no concrete actuation surface — Important x 2. |
| Fabricated-defect claims | Finding | rev8 changelog claims rev7 had "two competing Context blocks" (false); claims `### 1.` was dropped (was never there) — Critical 2 + Minor. |

---

**Cross-checks against rev8-claimed fixes:**

| rev8 claim | Verified? | Note |
|---|---|---|
| ADR Status returned to bare `Accepted` per repo precedent | PASS | Line 2463: bare `Accepted`. Matches all 5 prior ADRs. |
| Provenance moved to `**Provenance:**` line in Context section | PASS | Line 2471: `**Provenance:** Decided by Claude (autonomous-pipeline rev3-rev8) — …`. |
| `unsetFingerprintPlaceholder` declaration hoisted into T1.5 | PASS | Line 668: declares the constant in T1.5 Step 3 §3 with code block. |
| T1.5 Files section extended (`compute_drift.go` + `compute_drift_test.go`) | PASS | Lines 597-598. |
| Open Questions default changed to halt-and-surface | PARTIAL | Direction correct (line 168); enforcement mechanism unspecified (Important). C-1 incorrectly claimed downstream-blocked (Critical 1). |
| PR-count contradiction at line 2797 corrected to 12 | PASS | Line 2811 now reads `After all 12 PRs merge:`. |
| Open Questions ratification ask gains markdown checkboxes | PASS-VISUAL | Lines 165-166 have checkboxes; click-surface unspecified (Important). |
| W-3a rebase strategy: workspace-memory file reference replaced with inline summary | PASS | Line 1093: inline summary of `git pull --ff-only origin <parent-branch>` rule replaces the workspace-memory cross-reference. |
| rev7 process discipline elevated to Task Conventions line | PASS | Line 179. Discipline mentions PR counts + task counts but didn't catch the actual 71→72 task-count discrepancy (Critical 3). |
| Open Questions `### 1.` numbering dropped | UNNECESSARY-FIX | Line 155 has no number (PASS for current state); but rev7 also had no number — the "fix" describes a false-defect (Minor). |
| ADR Context section deduplicated (rev7 had two competing blocks) | FABRICATED | rev7 had ONE Context block (verified via `git show 791494a`). Changelog claim invents a defect — Critical 2. |

---

**Options the author may not have considered:**

1. **Drop the "(downstream) C-1" parenthetical from the Open Questions default** — the dependency graph at line 2822-2824 explicitly says C-1 is gated on P-DO, not W-9. Only W-9 itself waits at the gate. Closes Critical 1.

2. **Delete the fabricated "two competing Context blocks" claim from the rev8 changelog (line 119).** Either drop the bullet entirely, or rewrite to describe what rev8 actually changed in the Context section (which appears to be: added the `**Provenance:**` line). Closes Critical 2.

3. **Recompute task count to 72** OR identify which task was actually removed to land at 71. Update Scope Manifest line 18 + rev5 changelog line 70 to reflect the corrected count. The "71" claim has propagated unchallenged through rev5-rev8. Closes Critical 3.

4. **Specify the halt-and-surface mechanism concretely** — what enforces the halt (CI workflow? executing-plans skill behavior?), who posts the PR comment, what the comment text is, what the resume signal looks like. The default is operationally meaningless without these. Closes Important on mechanism.

5. **Resolve the checkbox UX ambiguity** — either accept the checkboxes are visual-only + add explicit "User ratifies by replying to W-9 PR with `ratified`/`override`" instruction, OR require the executing-plans agent to copy the Open Questions block into the W-9 PR description (where checkboxes are click-actuated). Closes Important on checkbox UX.

6. **Replace T3.1.5's literal code-block re-statement with a cross-reference to T1.5** — single source of truth for the `unsetFingerprintPlaceholder` constant + `ComputeDrift` function body. Eliminates dual-update risk. Closes Important on T3.1.5 dual-source.

7. **Sweep `grep -n "this revision"` and remove 5 stale "(this revision)" markers** at lines 23, 53, 67, 73, 91 — leave only line 107 (rev8). Add a Task Conventions sweep line: "When a new revision lands, the prior-revision's `(this revision)` parenthetical is dropped." Closes Minor on annotation rot.

8. **Update or delete the stale rev7 changelog `Decided by:` reference** at line 101 — rev8 collapsed `Decided by:` into the `Provenance:` line, so the rev7 claim no longer matches the ADR body. Closes Important on changelog truth-decay.

9. **Drop the rev8 changelog `### 1.` Minor** OR rewrite to acknowledge the cycle-7-reviewer false-positive. The "1." was never present in rev7. Closes Minor on false-defect.

---

**Verdict reasoning:**

Three Critical findings: (1) the Open Questions default claims C-1 waits at the gate alongside W-9, but the dependency graph in the same plan (line 2822-2824), the rev4 sequencing changes block (line 2828), and the drafting/merge sequencing rules (lines 2854-2855) all explicitly say C-1 is downstream of P-DO, not W-9 — the rev8 fix invents a phantom downstream-block; (2) the rev8 Minor changelog (line 119) claims rev7 had "two competing `## Context` blocks" but verification via `git show 791494a:...md` shows rev7 had exactly ONE `## Context` heading at line 2406 — the changelog fabricates a defect that didn't exist, then claims to fix it (same false-defect bug-class as the cycle-7 "tooling compatibility preserved" claim that didn't preserve anything); (3) the Scope Manifest task-count claim of 71 is arithmetically wrong (72 + 1 + 1 - 2 = 72, not 71) and contradicted by counting visible task headings (62 visible + T7.2-T7.12 expansion of +10 = 72) — the same rev8 sweep-discipline (line 179) that explicitly mentions task-count cross-checks failed to catch this, on its first numerical claim. Plus four Important findings (rev7 changelog `Decided by:` reference now stale because rev8 collapsed the field; Open Questions halt mechanism unspecified — no enforcement surface; checkbox UX has no actuation path because committed `.md` checkboxes are visual-only; T1.5/T3.1.5 dual code-block creates drift risk). Plus three Minor findings (5 "(this revision)" leftover markers; false-positive `### 1.` Minor in rev8 changelog describing a non-existent rev7 defect; T3.0.5 historical reference in rev3 changelog).

Per the prompt's PASS criteria (zero Critical AND every Important addressed): **FAIL**. The rev8 changes correctly addressed cycle-7's Critical 1 (ADR Status returned to bare `Accepted` + Provenance line added) + Critical 2 (`unsetFingerprintPlaceholder` hoisted into T1.5; Files section extended) + Critical 3 (default direction corrected from silent-ratification to halt-and-surface) + multiple Important findings (PR-count fixed; rebase strategy inlined; process discipline elevated to Task Conventions). But rev8 introduced THREE new Critical defects directly attributable to its own changes: (a) the Open Questions C-1 dangling claim; (b) the fabricated "two Context blocks" changelog claim; (c) the persistent task-count arithmetic error that survived BECAUSE the new Task Conventions sweep-line failed to enforce its own rule.

Pattern observation across cycles 1-8: each rev fixes the prior cycle's surface defects but introduces 1-3 new ones — usually in the meta-fix layer (changelog-about-the-fix, sweep-discipline-about-future-fixes, mechanism-claims-about-the-fix). Cycle-3 caught YAGNI in the artifact; cycle-4 caught arithmetic in the artifact; cycle-5 caught attribution in the ADR; cycle-6 caught dangling cross-references; cycle-7 caught false tooling claims; cycle-8 catches: (a) cross-reference invention (C-1 added to halt scope without graph backing), (b) defect fabrication (Context blocks claim), (c) recursive process-discipline failure (the new sweep rule fails its first arithmetic claim). The deeper pattern: the longer this revision sequence runs, the more meta-content (changelog entries about changelog entries, sweep rules about sweep rules) accumulates, and meta-content has its own defect surface that compounds. Every additional changelog block is another claim that needs verification.

Recommended next step (rev9): apply Options 1, 2, 3 (the three Critical fixes) atomically; apply Options 4-6 to close the Important findings; consider Option 7 (process-fix for the "(this revision)" annotation rot). Before drafting rev9, run an explicit cross-validation pass: every changelog claim that describes "rev<N-1> had defect X; rev<N> fixes it" must be verified by `git show <rev-N-1-commit>:...md | grep <defect-pattern>` returning the defect-pattern. If the pattern is absent, the claim is fabricated and must be removed — not just rewritten. The rev8 author would have caught the fabricated "two Context blocks" claim with this discipline. Process recommendation: treat changelog entries as load-bearing claims subject to the same rigor as task bodies.
