### Adversarial Review Report

**Phase:** plan
**Cycle:** 7 (unbounded per user direction)
**Artifact:** `docs/plans/2026-05-03-iac-conformance-and-replace.md` (commit 791494a)
**Status:** FAIL

---

**Findings (Critical):**

- **[ADR Status value still does NOT preserve "single recognized value" tooling compatibility — rev7 misapplied cycle-6 Important Option (b)]** §rev7 changelog (line 101) and the ADR body (line 2402) set the Status to: `Accepted (autonomous-pipeline; user-ratification deferred — see plan § Open Questions § "ProviderPlanner deferral")`. The rev7 changelog claims this "Preserves ADR-status tooling compatibility (every prior ADR uses `Status: Accepted`)." Verified by reading `docs/adr/001` through `docs/adr/005`: every prior ADR uses **exactly** `## Status\nAccepted` — a single bare token on its own line, NO parenthetical content. The rev7 value is a multi-token sentence with a parenthetical that includes punctuation (`—`, `(`, `)`, `"`). A docs-validation script that does `grep -E '^Accepted$' docs/adr/*.md` matches all 5 prior ADRs but rejects ADR 006. A script that does `awk '/^## Status/{getline; print}'` returns `Accepted (autonomous-pipeline; ...)` for ADR 006 vs. bare `Accepted` for all others — diff-comparable but no longer equal. Cycle-6 reviewer's Option (b) verbatim was: `Accepted (autonomous-pipeline; user-ratification deferred — see plan § Open Questions)` "keeping the Status field a single recognized value." That recommendation was internally self-contradictory (the parenthetical IS the breaking-change vs. prior ADRs), and the rev7 author copied the suggestion text without verifying that it actually preserved tooling compatibility. **The cycle-6 finding is unaddressed**: either the Status must be the bare word `Accepted` (with the deferral note moving entirely to a new field like `Provenance:` or to the Context body), OR the rev7 changelog must NOT claim "tooling compatibility preserved" — it currently does both, ghost-writing tooling-safety claims that the file content contradicts. **Recommendation:** make the Status the bare word `Accepted` exactly per repo precedent. Move the parenthetical to a new line BELOW the Status block (e.g., a `**Provenance:** autonomous-pipeline rev3-rev7; user-ratification deferred — see plan § Open Questions § "ProviderPlanner deferral"` line in the ADR Context section). This is the actual realization of cycle-6 Important Option (b).

- **[T1.5 Files section does not list `compute_drift.go` or `compute_drift_test.go` despite both being created/modified by the task — `unsetFingerprintPlaceholder` declaration site untracked]** §T1.5 Files section (lines 575-579) lists 4 files: `errors.go` (Create), `diagnostic.go` (Create), `infra.go` (Modify line 1071), and `infra_apply_plan_test.go` (Test extend). But the rev6 ComputeDrift sentinel test (Step 3.5, line 656) and the rev7 `unsetFingerprintPlaceholder` constant (declared in the embedded code block at line 1396 within T3.1.5) both reference `iac/inputsnapshot/compute_drift.go` + `iac/inputsnapshot/compute_drift_test.go` as files in T1.5's package. Verified at line 702 (T1.5 Step 5 commit): `git add iac/inputsnapshot/errors.go iac/inputsnapshot/diagnostic.go iac/inputsnapshot/compute_drift.go iac/inputsnapshot/compute_drift_test.go cmd/wfctl/infra.go cmd/wfctl/infra_apply_plan_test.go` — the commit stages 6 files but the Files section listed only 4. Verified at line 647 (Step 3 §3): "compute_drift.go: func ComputeDrift(...) — pure function..." declares the function but the Files section never names the file. **The rev7 author claims (changelog line 98) "Introduced unexported `unsetFingerprintPlaceholder` constant in `iac/inputsnapshot/compute_drift.go`" — but the constant declaration appears at line 1396 inside T3.1.5's embedded code block, not in T1.5 where it's first referenced.** An implementer reading T1.5's Files section literally creates the wrong files; an implementer reading T1.5's Step 3 finds `compute_drift.go` referenced as one of three files; the implementer reading the test in Step 3.5 references `unsetFingerprintPlaceholder` but never finds the constant declaration in T1.5's body. The constant declaration belongs in T1.5 (where the test references it), not in T3.1.5 (where T3.1.5's body re-states it via a code block annotated `T1.5 — UPDATED in rev5/rev6/rev7`). **Recommendation:** (a) extend T1.5 Files section to list `iac/inputsnapshot/compute_drift.go` (Create) + `iac/inputsnapshot/compute_drift_test.go` (Create); (b) hoist the `unsetFingerprintPlaceholder` constant declaration from T3.1.5's code block (line 1396) into T1.5's Step 3 §3 prose where ComputeDrift is first specified; (c) cross-reference from T3.1.5 back to T1.5 ("constant declared in T1.5; referenced here for in-package context").

- **[Open Questions § "Default if no user input by W-9 merge time" contradicts the user-mandate "don't defer fixes"]** §Lines 150 (Open Questions) + 2387 (W-9 intro) + ADR 006 itself all say: "Default if no user input by W-9 merge time: the autonomous-pipeline ratifies its own interpretation and W-9 merges with the deferral." The user's mandate (verbatim from prompt): "Don't defer any fixes. Refactor if you have to. Let's build these fixes the right way." The autonomous-pipeline interprets ProviderPlanner as "speculative future-extension scaffolding" (not-a-fix), and uses that interpretation to justify the deferral. The Open Questions section then sets the **default to silent ratification** if the user doesn't intervene. **This is not a user-intent-aligned default**: a user who said "don't defer fixes" should encounter a default of "halt W-9 until user explicitly ratifies the deferral interpretation," not "ship-with-deferral-unless-stopped." The current default routes around the user's explicit framing while claiming to surface the question. The Open Questions section is theatrical: it surfaces the question to the user but pre-sets the default to "ratified by silence" — which the user did not consent to, and which the cycle-5 review explicitly caught as ghost-attribution in a different form. **Recommendation:** change the default to: "If the user has not explicitly ratified the deferral interpretation by the time W-9 is ready to merge, the autonomous-pipeline HALTS at the pre-merge gate and surfaces the question via PR comment (not via silent ratification). User must reply 'ratified' or 'override' before W-9 ships." This aligns the default with the user's "don't defer fixes" mandate and removes the silent-ratification escape hatch. Alternatively: drop W-9 from the autonomous-pipeline scope entirely (W-9 ships only after user ratification, requiring an explicit pause-and-surface in the executing-plans flow).

---

**Findings (Important):**

- **[PR-count contradiction: Scope Manifest says 12, Final Verification says 11]** §Line 17 (Scope Manifest): `**PR Count:** 12`. Line 2754 (Final Verification): `After all 11 PRs merge:`. The PR Grouping table (lines 119-132) has 12 entries (W-1, W-2, W-3a, W-3b, W-4, W-5, W-6, W-7, W-8, W-9, P-DO, C-1). Final Verification's "11 PRs" undercounts by 1. This contradiction predates rev7 (likely a residual from rev3's W-3 split into W-3a/W-3b), but rev7's process discipline ("every rename/relocate from rev7 onward MUST run grep -n") doesn't catch numerical contradictions because there's no surface to grep for. The cycle-6 reviewer's pattern observation about "self-contradicting changelog claims" extends to numerical claims as well. **Recommendation:** update line 2754 to `After all 12 PRs merge:`. Add a Task Conventions line: "**Numerical claims (PR counts, task counts, file-edit counts) must be verifiable by counting the source-of-truth (Scope Manifest table, Files sections). When a count changes, all dependent claims update.**" The cycle-6 process-fix discipline only covers string-rename sweeps, not arithmetic-claim sweeps.

- **[Open Questions section uses prose framing, not the checkbox-style ratification ask the cycle-6 reviewer recommended]** §Cycle-6 review Critical 1 recommendation: "with a one-paragraph summary AND a checkbox-style ask." The rev7 Open Questions section is prose only — no `- [ ] Ratify the deferral` / `- [ ] Override (build ProviderPlanner now)` checkboxes. Markdown checkboxes render as interactive UI on GitHub PRs, providing a literal click-to-ratify surface. Prose framing leaves the user to figure out how to communicate the choice. The rev7 author may have intentionally chosen prose (fine), but the changelog claim "Added top-level `## Open Questions` section after Scope Manifest with the ProviderPlanner-deferral ratification ask" implies an actual ask surface, which the prose-only form doesn't provide. **Recommendation:** add markdown checkboxes:
  ```markdown
  **User ratification ask (check exactly one):**
  - [ ] Ratify the deferral — W-9 ships as-is (CI gate only; ProviderPlanner deferred to first concrete consumer)
  - [ ] Override — W-9 expands to include ProviderPlanner interface (estimated +30 min plan revision + ~1 hour implementation)
  ```
  Now the question has a literal answering surface. Otherwise drop the changelog claim about the ratification ask.

- **[W-3a rebase strategy at line 1032 references workspace-memory file `feedback_worktree_agents_must_ff_before_commit.md` — implementer may not have access to that file]** §Line 1032 says the implementer performs the rebase "per `feedback_worktree_agents_must_ff_before_commit.md`." This is a file in the user's workspace memory (per the system reminder: `/Users/jon/.claude/projects/-Users-jon-workspace/memory/`). An implementer agent operating in an isolated worktree may not have access to the user's memory directory; the plan should be self-contained. **Recommendation:** either (a) summarize the relevant rule inline ("the implementer fast-forwards on the parent branch via `git pull --ff-only origin <parent-branch>` before each commit"), or (b) note that the reference is to user-workspace-memory and provide the inline summary as authoritative for plan execution.

- **[T3.0.4's "Step 4 triage path" note is added BEFORE Step 4 (line 1089), inserting cognitive load mid-task; cycle-6 reviewer recommendation was a "one-line note in T3.0.4's intro"]** §The rev7 implementation places the triage note at line 1089, AFTER Step 3 and BEFORE Step 4. Cycle-6 reviewer recommended adding a "one-line note in T3.0.4's intro" so the implementer reads the trade-off context before starting Steps 1-3, not after. The current placement also breaks the consistent Step 1-2-3-4-5 numbering convention used by every other task in the plan (e.g., T1.5 Steps 1-5 at lines 581-704). The triage note effectively becomes Step 3.5, which the implementer reads after writing tests + implementation but before running them. **Recommendation:** move the triage note from line 1089 to T3.0.4's intro (around line 1024 area, after the task header), as a "Note (rev7 — addresses cycle-6 ...)" block matching the pattern at T1.1 line 174 and T3.1.5 line 1273. Implementer reads it before starting Step 1.

- **[rev7 process fix lives only in changelog, not in Task Conventions — rev8 author may not see it]** §Lines 106-107 (rev7 process fix): "every rename/relocate from rev7 onward MUST run `grep -n "<old form>"` over the file before commit." This discipline is documented ONLY in the rev7 changelog block. A rev8 author scanning the active plan body (Task Conventions, Files sections, etc.) does not see the discipline; they may rediscover the same defect class. The rev7 author themselves missed several sweep gaps despite adopting the discipline (e.g., the unfixed PR-count contradiction at line 2754; the missing T1.5 Files entries for `compute_drift.go`). The discipline should be a first-class plan convention, not a changelog footnote. **Recommendation:** add a Task Conventions line: "**Sweep verification (rev7+):** every rename/relocate, file-path change, or numerical-count change MUST be verified by running `grep -n '<old form>'` over the plan AND `grep -nE '^####? ' | wc -l` for section/heading counts AND counting Scope Manifest tables vs. Final Verification claims. Every match either updates or is explicitly tagged as historical/changelog."

---

**Findings (Minor):**

- **[Open Questions intro says "the user encounters this section before any task body" — but Task Conventions appears between Open Questions and the task bodies]** §Line 140: "The user encounters this section before any task body, so the question cannot land in attribution-limbo." Verified by `grep -nE "^##? "`: the section ordering is Scope Manifest (15) → Open Questions (138) → Task Conventions (154) → PR W-1 (164). Strictly speaking, Task Conventions sits between Open Questions and the first task body, but Task Conventions is metadata not a task body, so the claim is technically true. Minor wording precision: "before any task body" is correct; an attentive reader who reads top-down hits Task Conventions first then PR W-1, but Open Questions surfaces before both. PASS, but the framing could be tightened.

- **[rev7 changelog line 99 says "rev6 changelog updated to include 006" — this rev7 change technically modifies historical changelog content]** §Line 82 (rev6 changelog): `(rev7 — added "006" to match the T9.1 body form per cycle-6 Important.)` — rev7 modified rev6's changelog entry by appending a parenthetical. This is stylistically OK (the parenthetical is clearly tagged as a rev7 modification), but it conflicts with the principle that changelog entries describe what THAT revision shipped — modifying rev6's entry post-hoc is weird. The cleaner pattern would be a rev7 changelog bullet stating "T9.1 commit-message form reconciled by editing T9.1 body, not rev6 changelog." **Recommendation:** none required; flag as a minor convention question.

- **[T9.1 boundary section's git commit message at line 2435 still uses `"006"` form, matching T9.1 body — PASS]** §Verified rev7's Important fix #3: T9.1 body line 2435 commit-message form = `ci(iac): cross-plugin build gate + ADR 006 for ProviderPlanner deferral`. rev6 changelog line 82 now includes "006" (after the rev7 inline edit). Both forms aligned. PASS.

- **[Open Questions intro section number is `### 1.` — no `### 2.` exists; numbering one-item sections is over-engineered]** §Line 142: `### 1. ProviderPlanner deferral — ratify or override`. Single-item sections don't need numbering. Either drop the "1." (cleaner: `### ProviderPlanner deferral — ratify or override`) or commit to numbered sections + add "Future open questions: none currently." This is preference-level. **Recommendation:** none required; flag for consistency if other open questions land later.

---

**Bug-class scan transcript:**

| Class | Result | Note |
|---|---|---|
| Unstated assumptions | Finding | rev7 ADR Status claim "Preserves tooling compatibility" is unverified — Critical 1. |
| Repo-precedent conflicts | Finding | ADR Status format diverges from 5 prior ADRs — Critical 1. |
| YAGNI violations | Clean | ProviderPlanner remains dropped per cycle-3. |
| Missing failure modes | Finding | `unsetFingerprintPlaceholder` declaration site disconnected from declaring task — Critical 2. |
| Security / privacy | Clean | rev7 sentinel sweep complete (3 capitalized residuals fixed). |
| Rollback story | Clean | T3.0.4 + T3.1.5 + T3.4 rollback notes correctly couple the W-3a unit. |
| Simpler alternative not considered | Finding | Open Questions silent-ratification default — Critical 3. |
| User-intent drift | Finding | "Default ratifies its own interpretation" violates "don't defer fixes" — Critical 3. |
| Over-decomposition | Clean | rev7 task count = 71 unchanged. |
| Verification-class mismatch | Clean | rev6 plan-author-time verification preserved. |
| Hidden serial dependencies | Finding | T1.5 Files section missing `compute_drift.go` (Critical 2 sub); W-3a rebase references workspace-memory file (Important). |
| Missing rollback wiring | Clean | Per-task rollbacks present including T3.0.4. |
| Cost claims math | Clean | Smoke-gate cost remains $0.0005/PR. |
| Internal consistency / changelog truthfulness | Finding | PR-count contradiction (12 vs 11) at line 2754 — Important. ADR Status claim contradicts file content — Critical 1. |
| Dangling cross-references | Clean | `decisions/0001-...`, `PreservedFingerprint`, `T9.3` all resolved or historical-only. ADR cross-ref `plan § Open Questions § "ProviderPlanner deferral"` resolves. |
| Sweep completeness | Finding | T1.5 Files section sweep missed `compute_drift.go`; PR-count sweep missed line 2754 — Critical 2 + Important. |
| Test brittleness | Clean | rev7 `unsetFingerprintPlaceholder` constant decouples test from literal `"(unset)"`. |
| Default-policy alignment | Finding | Open Questions silent-ratification default — Critical 3. |

---

**Cross-checks against rev7-claimed fixes:**

| rev7 claim | Verified? | Note |
|---|---|---|
| Top-level `## Open Questions` section added after Scope Manifest | PASS | Lines 138-152. ADR + W-9 intro cross-references resolve. |
| Swept all 3 stale `decisions/0001-...` references | PASS | `grep -n "decisions/0001"` returns 4 hits: 3 in changelog (63, 75, 94), 1 in W-9 Files section as historical relocation note (2393), and the explicit "Do NOT use" anti-instruction at line 2568. All active references point at `docs/adr/006-...`. |
| Applied rev4 changelog grouping (Critical/Important/Minor sub-headers) | PASS | Lines 38, 42, 50 have sub-headers. 3 Critical / 7 Important / 2 Minor. |
| Swept all 3 `PreservedFingerprint` capitalized residuals | PASS | `grep -n "PreservedFingerprint"` returns only changelog refs (lines 55, 80, 97). T1.2 Step 3 prose, commit message, and T1.5 §3 prose all use lowercase. |
| Decoupled ComputeDrift test from literal `"(unset)"` via `unsetFingerprintPlaceholder` | PARTIAL | Constant declaration exists at line 1396 in T3.1.5's code block; tests at lines 689-690 reference it. BUT the constant is declared in T3.1.5's body, not in T1.5 where the test references it; T1.5 Files section doesn't list `compute_drift.go` (Critical 2). |
| ADR Status changed to `Accepted (autonomous-pipeline; user-ratification deferred — see plan § Open Questions § "ProviderPlanner deferral")` | PARTIAL | Verified at line 2402; text matches changelog claim. BUT the parenthetical breaks the bare-`Accepted` repo precedent — claim "Preserves ADR-status tooling compatibility" is FALSE (Critical 1). |
| T9.1 commit-message form reconciled (rev6 changelog includes "006") | PASS | Line 82 updated; line 2435 commit message includes "006". |
| W-3a rebase strategy extended (WHO + W-1 prerequisite + conflict resolution) | PARTIAL | Lines 1030-1033 cover all 3. BUT references workspace-memory file `feedback_worktree_agents_must_ff_before_commit.md` (Important). |
| T3.0.4 gains Step 4 triage path note | PARTIAL | Note exists at line 1089. BUT placed mid-task (after Step 3, before Step 4) instead of in the intro per cycle-6 recommendation (Important). |
| rev7 placeholder dropped | PASS | No `rev8 (if needed)` or similar placeholder found. |
| rev4 changelog line 41 wording rewritten | PASS | Line 41 (now in Critical fixes block) describes rev4 actual delivery: "ProviderPlanner interface DROPPED entirely … Field-placement intent recorded; rev5 moved field declarations to W-3a." |
| Process fix: every rename/relocate runs `grep -n` | PARTIAL | Discipline documented at line 106 BUT only in changelog; not elevated to Task Conventions. The PR-count contradiction (12 vs 11 at line 2754) shows the discipline doesn't catch numerical claims (Important). |

---

**Options the author may not have considered:**

1. **Make the ADR Status the bare word `Accepted`** exactly per repo precedent. Move the deferral note to a new `**Provenance:**` line in the Context section. This actually realizes cycle-6 Important Option (b)'s intent: "keeping the Status field a single recognized value." The current parenthetical-extended Status only nominally implements the recommendation. Closes Critical 1.

2. **Hoist the `unsetFingerprintPlaceholder` constant declaration into T1.5's body** (where the test references it). Extend T1.5 Files section to include `iac/inputsnapshot/compute_drift.go` (Create) + `iac/inputsnapshot/compute_drift_test.go` (Create). Cross-reference from T3.1.5 back to T1.5. Closes Critical 2.

3. **Change the Open Questions default from silent ratification to halt-and-surface.** "If the user has not explicitly ratified the deferral interpretation by W-9's pre-merge gate, the autonomous-pipeline HALTS and surfaces via PR comment. User must reply 'ratified' or 'override' before W-9 ships." Aligns the default with "don't defer fixes." Closes Critical 3.

4. **Update PR-count from 11 to 12 at line 2754** (Final Verification). Add a Task Conventions line about numerical-claim sweeps. Closes Important on PR-count contradiction.

5. **Add markdown checkboxes to the Open Questions § ratification ask** — `- [ ] Ratify the deferral` / `- [ ] Override (build ProviderPlanner now)`. Provides a literal answering surface. Closes Important on prose-only framing.

6. **Inline-summarize the workspace-memory rule** at line 1032 instead of cross-referencing the user's memory file: "the implementer fast-forwards on the parent branch via `git pull --ff-only origin <parent-branch>` before each commit." Plan stays self-contained. Closes Important on memory-file dependency.

7. **Move T3.0.4's Step 4 triage path note from line 1089 to T3.0.4's intro** (around line 1024 area). Implementer reads the trade-off context before starting Steps 1-3. Closes Important on placement.

8. **Elevate the rev7 process discipline from changelog to Task Conventions** as a first-class plan rule. Extend the discipline to numerical-count sweeps (PR counts, task counts). Closes Important on rev8-author discoverability.

9. **Drop the "1." numbering from `### 1. ProviderPlanner deferral`** if no other open questions exist. Or add "Future open questions: none currently" so the numbered scheme is not solitary. Minor.

---

**Verdict reasoning:**

Three Critical findings: (1) the rev7 ADR Status `Accepted (autonomous-pipeline; user-ratification deferred — see plan § Open Questions § "ProviderPlanner deferral")` is a multi-token line with parenthetical content, NOT the bare `Accepted` value used by all 5 prior ADRs; the rev7 changelog claim "Preserves ADR-status tooling compatibility" is therefore false-positive — a docs-validation script doing exact-line-match would reject ADR 006 while accepting all 5 prior ADRs; (2) T1.5's Files section omits `iac/inputsnapshot/compute_drift.go` (Create) and `iac/inputsnapshot/compute_drift_test.go` (Create) despite both being committed in T1.5's Step 5 (line 702); the rev7 `unsetFingerprintPlaceholder` constant is declared at line 1396 inside T3.1.5's code block instead of in T1.5 where the test (Step 3.5, line 689) references it — the declaration site is disconnected from the declaring task; (3) the Open Questions section's default ("autonomous-pipeline ratifies its own interpretation and W-9 merges with the deferral") contradicts the user's explicit mandate "don't defer fixes" — a user who said "don't defer" would expect the default to be "halt and surface for ratification," not "ship-with-deferral-unless-stopped." The Open Questions section is theatrical: it surfaces the question but pre-sets the default to silent ratification, which the user did not consent to. Plus five Important findings (PR-count contradiction 12 vs 11 at Final Verification line 2754; Open Questions uses prose framing not the cycle-6-recommended checkbox-style ask; W-3a rebase strategy references workspace-memory file the implementer may not have access to; T3.0.4 Step 4 triage path placed mid-task instead of in intro per cycle-6 recommendation; rev7 process discipline lives only in changelog not Task Conventions). Plus four Minor findings.

Per the prompt's PASS criteria (zero Critical AND every Important addressed): **FAIL**. The rev7 changes correctly addressed cycle-6's Critical 1 (Open Questions section now exists + cross-references resolve) + Critical 2 (`decisions/0001-...` sweep complete) + Critical 3 (rev4 changelog grouping applied) + multiple Important findings (PreservedFingerprint sweep complete; ComputeDrift test decoupled via constant; T9.1 commit-message reconciled; W-3a rebase strategy extended). But rev7 introduced THREE new Critical defects directly attributable to its own changes: (a) the ADR Status "fix" applied cycle-6 Option (b) verbatim without verifying the parenthetical preserves bare-token-tooling compatibility; (b) the `unsetFingerprintPlaceholder` constant landed in T3.1.5's code block, not T1.5 where the test references it, AND T1.5's Files section was not updated to include `compute_drift.go`; (c) the Open Questions default of silent-ratification contradicts the user's "don't defer fixes" framing — the section nominally surfaces the question but pre-sets the default to "ratify-by-silence," ghost-attributing user consent in a different form than cycle-5 caught.

Pattern observation across cycles 1-7: rev7 explicitly adopted a "sweep verification" discipline for string renames (and applied it to `decisions/0001`, `PreservedFingerprint`, `T9.3` — all PASS). But the sweep discipline does NOT cover: (a) numerical-claim sweeps (PR count 12 vs 11 missed); (b) Files-section-vs-code-block sweeps (T1.5 Files section missed `compute_drift.go`); (c) declaration-site-vs-reference-site placement (constant in T3.1.5 vs test in T1.5); (d) verification of claimed properties (ADR Status "preserves tooling compatibility" claim not verified by actually grepping the 5 prior ADRs and comparing). The cycle-6 reviewer's pattern observation about "incomplete sweeps + dangling cross-references + self-contradicting claims" extends BEYOND string renames to the broader bug-class of "post-hoc claim verification."

Recommended next step (rev8): apply Options 1, 2, 3 (the three Critical fixes) atomically; apply Options 4-8 to close the Important findings. Specifically: (1) make ADR Status bare `Accepted` + move provenance to a separate Context line; (2) hoist `unsetFingerprintPlaceholder` constant declaration into T1.5 + extend T1.5 Files section to include `compute_drift.go`; (3) change Open Questions default from silent ratification to halt-and-surface. Process recommendation: extend the rev7 sweep discipline to ALSO cover numerical-claim sweeps + Files-section-vs-code-block consistency + declaration-site-vs-reference-site placement + verification of claimed properties. The discipline should be a Task Conventions line, not a changelog footnote.
