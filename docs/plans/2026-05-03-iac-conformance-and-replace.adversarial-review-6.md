### Adversarial Review Report

**Phase:** plan
**Cycle:** 6 (unbounded per user direction)
**Artifact:** `docs/plans/2026-05-03-iac-conformance-and-replace.md` (commit 7a3fa80)
**Status:** FAIL

---

**Findings (Critical):**

- **[Dangling cross-reference + missing "open question" surface for ADR ratification]** §The rev6 ADR (lines 2342-2343) and the rev6 changelog (line 74) BOTH cross-reference `plan §W-9 § "Pending user ratification"` as the place the user is supposed to find and ratify the ProviderPlanner deferral. **No such section exists in the plan.** Verified via `grep -n "^##\|^###" .../iac-conformance-and-replace.md | grep -i "pending\|ratif\|open"` — zero matches. Verified via `grep -n "Pending user ratification"` — only 2 matches, both pointing AT the non-existent section. The plan also has no top-level `## Open Questions` section. So: rev6 corrected the ADR's `Decided by` from ghost-attribution to honest provenance (good), correctly downgraded the Status to `Provisional — awaiting user ratification` (good), but then routed the ratification through a section that does not exist. The user reading the plan top-down has no encountered prompt to ratify; the ADR (which only lives inside a fenced code block in T9.1, near line 2342, deep in the document) is the only place the question surfaces, and it points back at a non-existent anchor — a closed loop that never reaches the user. **Net effect:** rev6 nominally addresses cycle-5 Critical 2 (ghost-written user consent) but fails to actually surface the question for ratification. The deferral is in honest-attribution limbo with no operational path to becoming a real decision. **Recommendation:** add a top-level `## Open Questions` section immediately after `## Scope Manifest` (lines 15-117) listing: (1) "ProviderPlanner deferral — ratify or override (W-9; ADR 006)" with a one-paragraph summary AND a checkbox-style ask, and (2) any future open questions that arise. Cross-reference this section from the ADR (`See plan § Open Questions § "ProviderPlanner deferral"`) and from W-9's intro note. The surface to the user MUST be at the top of the plan where they encounter it before reading any task body. Without this, the rev6 "honest provenance" is theater.

- **[Stale `decisions/0001-...` references in active task bodies — rev6 sweep was incomplete]** §rev6 changelog (line 73) claims the ADR is relocated to `docs/adr/006-...md`, and T9.1 (line 2334) correctly lists the new path. But three active references to the OLD path survive in task bodies (NOT in the rev5 changelog history, where retention is appropriate):
  - **Line 2328** (W-9 intro): `> **Decision record (rev5):** see `decisions/0001-providerplanner-deferred-to-first-consumer.md` for the recorded reasoning.` — points readers at the wrong location.
  - **Line 2507** (TP1 Step 4): `record that decision as `decisions/<NNNN>-do-iac-v2-migration.md` per `recording-decisions/SKILL.md`` — instructs the IMPLEMENTER to file FUTURE ADRs at the wrong path. This is the same repo-precedent conflict cycle-5 caught for the rev6 ADR, but the TP1 instruction is unfixed. The implementer following the plan would create `decisions/0001-do-iac-v2-migration.md` (matching the skill convention literally) and re-introduce the cycle-5 Critical 1 defect in the codemod ADR.
  - **Line 2710** (Final verification cross-PR note): `ADR `decisions/0001-providerplanner-deferred-to-first-consumer.md` records the cycle-4 reasoning` — final-verification readers see the wrong path.

  The rev6 sweep updated only the rev6 changelog (line 73) and T9.1's Files+Step 0 (lines 2334-2366); it missed the W-9 intro note, the TP1 implementer instruction, and the Final Verification reference. Verified via `grep -n "decisions/0001\|decisions/" .../iac-conformance-and-replace.md` — 6 hits, of which 3 are stale references in active prose (lines 2328, 2507, 2710), 2 are in the rev5/rev6 changelog (intentional history), and 1 is the rev6 changelog's own note about the relocation. **This is the same bug-class that cycle-5 Critical 1 caught for the ADR itself**: rev N fixes the surface defect but fails to sweep all repo-precedent references. **Recommendation:** sweep all 3 active references to point at `docs/adr/006-...` (W-9 intro, Final Verification) AND fix line 2507's TP1 instruction to "record that decision as `docs/adr/NNN-do-iac-v2-migration.md` per repo precedent (the workflow repo uses `docs/adr/` with 3-digit numbering and Nygard-3 format; consult the existing 005-field-contracts.md as a template); the next free ADR number after T9.1's 006 is 007."

- **[rev6 changelog claims a fix it did not apply: "rev4 changelog block converted to grouped Critical/Important/Minor format"]** §line 84: `- rev4 changelog block converted to grouped Critical/Important/Minor format (matches rev5/rev6 convention).` — but rev4 changelog (lines 37-50) IS STILL FLAT. Verified by reading lines 37-50: 14 bullets, no `**Critical fixes:**` / `**Important fixes:**` / `**Minor fixes:**` sub-headers. The rev5 changelog (lines 51-70) IS grouped (lines 52, 56, 64). The rev6 changelog (lines 71-88) IS grouped (lines 72, 76, 83). Only rev4 remains flat. The rev6 author wrote the changelog claim ("rev4 changelog block converted to grouped...") without actually applying the change to the rev4 block. This is a self-contradicting changelog entry — the kind of defect that, six months later, a reader sees and loses trust in every other claim in the changelog. **Recommendation:** EITHER (a) actually apply the grouping to lines 37-50 by sorting the 14 bullets into Critical (T1.5 split, ProviderPlanner drop, plan.InputNames fix) / Important (W-9→P-DO edge, github-script swap, T7.14 dedup, panic recovery, TC1.5 budget alert, T7.13 cache backend, W-3b ordering) / Minor (T9.3 paths filter, T9.3 wording, field-add cross-ref); OR (b) remove the changelog claim. Don't ship a changelog entry that contradicts the file content.

---

**Findings (Important):**

- **[`PreservedFingerprint` capitalized references survive in active prose + commit message after rev6 unexport]** §rev6 changelog (line 78) claims `PreservedFingerprint` is unexported to `preservedFingerprint` and lists call-site updates. Verified the constant declaration (line 355) IS unexported and Compute body (lines 319, 323) uses lowercase. BUT three active references to the capitalized form remain:
  - **Line 295** (T1.2 Step 3 header prose): `**Step 3: Implement (rev5 — adds PreservedFingerprint sentinel + NewTolerantEnvProvider for the in-process apply postcondition contract)**` — capitalized in the section header.
  - **Line 414** (T1.2 commit message): `git commit -m "feat(iac): add inputsnapshot.Compute + Snapshot + NewTolerantEnvProvider + PreservedFingerprint sentinel"` — the commit message ships the OLD capitalized name. Implementer following the plan literally would commit a message that doesn't match the in-package symbol.
  - **Line 614** (T1.5 §3 prose): `rev5: honors `inputsnapshot.PreservedFingerprint` sentinel — if `applySnap[k] == PreservedFingerprint`, skip drift detection for that key` — both the package-qualified reference AND the unqualified comparison use the capitalized form. The package-qualified form `inputsnapshot.PreservedFingerprint` is now uncompilable (it would fail with `undefined: inputsnapshot.PreservedFingerprint`).

  The rev6 sweep updated the constant declaration + the function bodies but missed the prose + the commit message. **Recommendation:** sweep all 3 to lowercase. Specifically: line 295 → `(rev5/rev6 — adds preservedFingerprint sentinel + ...)`; line 414 commit message → `feat(iac): add inputsnapshot.Compute + Snapshot + NewTolerantEnvProvider + preservedFingerprint sentinel` OR use a description that doesn't expose the unexported name (`feat(iac): add inputsnapshot.Compute + Snapshot + NewTolerantEnvProvider with preservation sentinel`); line 614 → `rev5/rev6: honors the in-package preservedFingerprint sentinel — if applySnap[k] equals it, skip drift detection`. The commit message in particular is high-leverage: an implementer copy-pasting it ships a misleading log line.

- **[T1.5 ComputeDrift sentinel test brittle to literal-string change in `(unset)` placeholder]** §T1.5 Step 3.5 (lines 643-650) adds `TestComputeDrift_KeyMissingInApplySnap_ReportsDrift` which asserts `drift[0].ApplyFingerprint != "(unset)"`. The literal `"(unset)"` is sourced from ComputeDrift's body at line 1351 (`Name: name, PlanFingerprint: planFP, ApplyFingerprint: "(unset)"`). The test couples to that literal string. If a future maintainer normalizes the placeholder to `"<unset>"` or `"missing"` or `""` (empty string + a separate boolean), the test fails not because behavior changed but because the literal string changed. The same coupling bites the FormatStaleError formatter (line 607-613) which presumably also displays the placeholder. **Recommendation:** EITHER (a) introduce a package-level constant `inputsnapshot.UnsetFingerprintPlaceholder = "(unset)"` (or unexported `unsetFingerprintPlaceholder` for in-package access only) and reference the constant from both ComputeDrift's body and the test, OR (b) test the BEHAVIOR (drift entry exists, ApplyFingerprint differs from PlanFingerprint, len(drift)==1) without asserting the literal string. Option (b) is simpler — `if drift[0].ApplyFingerprint == drift[0].PlanFingerprint` + `if drift[0].PlanFingerprint == ""` are both stronger than literal-string equality. The cycle-5 reviewer raised this concern as a Specific Angle (#9) and rev6 added the brittle assertion anyway.

- **[T9.1 commit message references "ADR 006" but the rev6 changelog and W-9 intro both say the boundary commit is `ci(iac): cross-plugin build gate + ADR for ProviderPlanner deferral` (no "006")]** §line 2374 (T9.1 boundary section): `git commit -m "ci(iac): cross-plugin build gate + ADR 006 for ProviderPlanner deferral"`. line 80 (rev6 changelog): `(`ci(iac): cross-plugin build gate + ADR for ProviderPlanner deferral`)`. line 2424 (Step 5 reference): doesn't quote a message, just references "the boundary section above." Mismatch between the rev6 changelog claim and the actual T9.1 boundary section: changelog says NO "006" in the commit message, T9.1 body INCLUDES "006." The implementer following the plan literally has two competing messages. Minor inconsistency but the changelog is misleading. **Recommendation:** decide (the boundary-section form including "006" is more informative; update the changelog to match) or pick the changelog form (drop "006" from the boundary section since the ADR slot is identifiable from the file path in `git add`). One source of truth.

- **[W-3a rebase strategy paragraph silent on WHO performs the rebase + how cross-PR file overlaps coordinate]** §line 986 (W-3a rebase strategy section, rev6) explains commit-boundary preservation but doesn't say WHO performs the rebase (implementer? team-lead? the executing-plans skill?). For an autonomous-pipeline agent, the answer is "the implementer performs the rebase as part of the receiving-code-review skill," but the plan doesn't say so. Also: T1.1 in W-1 ALSO modifies `interfaces/iac_state.go` (adds IaCPlan + PlanAction fields). If W-1 and W-3a are both open concurrently and W-1 lands first, W-3a's T3.0.4 must rebase on W-1's tip. The plan's Sequencing rules (search for `Sequencing rule` in the file — they appear earlier) state W-1 → W-3a, but the rebase strategy paragraph at line 986 is silent on the W-1 prerequisite. **Recommendation:** extend line 986's paragraph: "Prerequisite: W-1 must merge first (per Sequencing rule #N — adds IaCPlan + PlanAction fields to the same file). T3.0.4 rebases on W-1's tip before adding the 3 ApplyResult fields. The implementer performs the rebase per the receiving-code-review skill; the team-lead does NOT perform per-implementer rebases (per `feedback_worktree_agents_must_ff_before_commit.md` — each agent is responsible for fast-forwarding before each commit)."

- **[ADR `Status` value "Provisional — awaiting user ratification" is novel — no prior ADR in the repo uses any non-`Accepted` status]** §all 5 prior ADRs use exactly `## Status\nAccepted`. rev6's ADR uses `## Status\nProvisional — awaiting user ratification. See plan §W-9 ...` (multi-paragraph status with cross-reference). The cycle-6 angles called this out: does "Provisional" break any tooling that grep-scans `Status: Accepted` (e.g., a docs-validation script, or a `# Active ADRs` table generator)? Verified: a tree search of the workflow repo for ADR-status-scanning tooling would resolve the question. The author did not document whether such tooling exists. The risk: a script that ignores any ADR not marked Accepted would silently exclude ADR 006 from any "active decisions" listing; conversely, a script that scans for the Status block existing at all would include it. The cycle-5 reviewer's recommendation was Option (b) (`Decided by: Claude (autonomous-pipeline rev5; user did not explicitly approve)` + a Status note). rev6 adopted both (a) honest provenance AND (b) Provisional status, but the Provisional status is novel and the impact of using a novel ADR status is undocumented. **Recommendation:** EITHER (a) document in the rev6 plan body (or in the ADR itself) that there is NO ADR-status-scanning tooling that requires Accepted (and verify by grepping the workflow repo for `Status: Accepted` or `Status:.*Accepted`), OR (b) change the Status to `Accepted (autonomous-pipeline; user-ratification deferred — see plan § Open Questions)` keeping the Status field a single recognized value. Option (b) keeps tooling compatibility AND honestly attributes provenance in the body. Option (a) is honest but requires the verification step.

- **[Numbering/grouping nit — T3.0.4 declares 3 fields in one task body — could be 3 separate task lines for granular review]** §T3.0.4 (lines 1034-1108) ships 3 field additions + 3 roundtrip tests in one task body. Reasonable size, OR could be 3 separate tasks (T3.0.4a InitialInputSnapshot, T3.0.4b InputDriftReport, T3.0.4c ReplaceIDMap) for per-field reviewer scrutiny. Cycle-5 Option 4 (which rev6 implemented) was about Git-history cleanliness — single commit modifies `interfaces/iac_state.go` once in W-3a — and that goal is met. But: per-field testing means the implementer must write all 3 tests + add all 3 fields atomically; if Step 1's first test fails because the second field's test mistakenly references the third field, the implementer triages 3 changes at once. The 3-tests-pass step (Step 4) has no per-field intermediate. Reasonable trade-off, but worth surfacing as a deliberate choice. **Recommendation:** add a one-line note in T3.0.4's intro: "Three fields added atomically per cycle-5 Option 4 (single `interfaces/iac_state.go` modification in W-3a). Implementer adds all 3 fields + 3 tests in Step 1-3 before running Step 4. If Step 4 fails, triage by isolating one field at a time via `go test -run TestApplyResult_<FieldName>_RoundTrip`." Acknowledges the trade-off + names the diagnostic path.

---

**Findings (Minor):**

- **[Documentation]** §line 89 retains `- rev7 (if cycle-6 review surfaces findings) — further revisions per cycle-6 review.` — same pre-emptive ghost-writing pattern that cycle-5 Important caught for `rev6 (if needed)`. rev6 removed the rev6 placeholder (per its own changelog line 85) but retained the rev7 placeholder. **Recommendation:** drop line 89. If cycle-6 surfaces findings, the rev7 author writes the rev7 entry. Same finding rev6 acknowledged for rev6.

- **[Documentation]** §line 41 still references the rev4 author's earlier (incorrect) field-add intent without crisp wording: "Field-add sites corrected (rev4 intent; rev5 implementation moved fields to W-3a)" — confusing because the line is in the rev4 changelog block. The rev4 changelog should describe what rev4 actually shipped, not what later revisions did. **Recommendation:** rewrite line 41 to describe rev4's actual delivery: "Field-add sites updated to a placeholder pending W-3a placement decision; final placement deferred to rev5."

- **[Documentation]** §line 117 (`**Status:** Draft`) — every prior ADR uses `## Status\nAccepted` (section heading, not bold-prefix). The plan's own `**Status:** Draft` line uses the rev5 ADR format that cycle-5 explicitly flagged as wrong. Plan-status convention is independent of ADR convention, but the bold-prefix style is the same form cycle-5 said NOT to use for ADRs. Minor consistency nit; the plan's status line predates the cycle-5 finding so no regression. **Recommendation:** none required; flag for the executing-plans skill if it ever grep-scans `**Status:**`.

- **[Documentation]** §the rev6 changelog (line 87) says `T7.13 cache YAML inline comment specifies the cache path explicitly.` — verified by reading line 2130-2160 area. The cache YAML inline comment now specifies `path: /tmp/budget-result.json` near the cache step. PASS.

---

**Bug-class scan transcript:**

| Class | Result | Note |
|---|---|---|
| Unstated assumptions | Clean | rev6 added the plan-time pre-condition verification for `interfaces/iac_state.go::ApplyResult` (line 143), removing the runtime grep. |
| Repo-precedent conflicts | Finding | (a) ADR Status "Provisional" novel — no prior ADR uses any non-`Accepted` status (Important); (b) line 2507 TP1 still instructs implementers to file ADRs at `decisions/<NNNN>-...` per skill convention, repeating cycle-5 Critical 1 (Critical 2). |
| YAGNI violations | Clean | ProviderPlanner remains dropped per cycle-3. |
| Missing failure modes | Clean | rev6 added 3 ComputeDrift unit tests in T1.5 (line 624-650). |
| Security / privacy | Clean | rev6 unexported PreservedFingerprint to preservedFingerprint (constant declaration + function bodies); 3 stale capitalized refs in prose/commit-message remain (Important). |
| Rollback story | Clean | T3.0.4 + T3.1.5 + T3.4 rollback notes correctly couple the W-3a unit. |
| Simpler alternative not considered | Clean | rev6 consolidated all 3 ApplyResult fields into T3.0.4 per cycle-5 Option 4. |
| User-intent drift | Finding | rev6 honest-attribution lands the ADR in user-ratification limbo with no operational surface for the user to encounter the question (Critical 1). |
| Over-decomposition | Clean | rev6 task count = 71 unchanged. |
| Verification-class mismatch | Clean | rev6 removed T1.1's runtime grep; replaced with plan-author-time verification at line 143. |
| Hidden serial dependencies | Finding | W-3a rebase strategy at line 986 is silent on WHO performs rebase + cross-PR file overlap with W-1's `interfaces/iac_state.go` modifications (Important). |
| Missing rollback wiring | Clean | Per-task rollbacks present including T3.0.4. |
| Cost claims math | Clean | Smoke-gate cost corrected to $0.0005/PR at all 3 active references (lines 82, 2130, 2622). |
| Internal consistency / changelog truthfulness | Finding | rev6 changelog claims rev4 grouping fix not actually applied (Critical 3); rev6 changelog's T9.1 commit message form differs from T9.1's body form (Important). |
| Dangling cross-references | Finding | ADR cross-refs `plan §W-9 § "Pending user ratification"` — section does not exist (Critical 1). |
| Test brittleness | Finding | T1.5 ComputeDrift test asserts literal `"(unset)"` placeholder (Important). |

---

**Cross-checks against rev6-claimed fixes:**

| rev6 claim | Verified? | Note |
|---|---|---|
| ADR relocated to `docs/adr/006-providerplanner-deferred-to-first-consumer.md` | PARTIAL | T9.1 body line 2334 + Step 0 ADR target the new path; W-9 intro line 2328, TP1 line 2507, Final-Verification line 2710 still reference the OLD `decisions/0001-...` path (Critical 2). |
| ADR uses Nygard-3 format `## Status` / `## Context` / `## Decision` / `## Consequences` | PASS | Verified at lines 2342/2345/2355/2359 (inside fenced markdown code block; no TOC pollution). |
| Status `Provisional — awaiting user ratification` | PASS-ish | Status field set; novel value vs. repo-precedent of `Accepted` (Important). |
| `Decided by: Claude (autonomous-pipeline rev3-rev6; user did not explicitly approve)` | PASS | Verified at line 2353. But the plan still has no Open Questions section to surface ratification (Critical 1). |
| T1.1 runtime pre-step grep removed; verified at plan-revision time | PASS | Lines 142-143 confirm verification done; no runtime grep instruction. |
| All 3 ApplyResult fields in T3.0.4 | PASS | Lines 1081-1097 declare InitialInputSnapshot + InputDriftReport + ReplaceIDMap. T3.4 (line 1485) only POPULATES, no separate Modify of `interfaces/iac_state.go`. |
| `PreservedFingerprint` unexported to `preservedFingerprint` | PARTIAL | Constant declaration (line 355) + function bodies (lines 319, 323, 374, etc.) use lowercase. 3 prose/commit-message references remain capitalized (line 295, 414, 614 — Important). |
| ComputeDrift sentinel test added to T1.5 (3 unit tests in Step 3.5) | PASS | Lines 624-650 contain TestComputeDrift_PreservedSentinelSkipsDrift + DifferentFingerprint_ReportsDrift + KeyMissingInApplySnap_ReportsDrift. KeyMissing test brittle to literal `"(unset)"` (Important). |
| T9.1 commit boundary explicit (single commit) | PASS | Line 2370-2375 specifies single commit; line 2424 references the boundary section. Commit-message text differs between changelog (line 80) and body (line 2374) on whether "006" is included (Important). |
| W-3a rebase strategy documented | PASS-ish | Line 986 documents the strategy but is silent on WHO performs the rebase + cross-PR W-1 prerequisite (Important). |
| Smoke-gate cost corrected to $0.0005/PR | PASS | All 3 active references (lines 82, 2130, 2622) say $0.0005. No $0.005 residuals in active prose. |
| rev4 changelog grouped Critical/Important/Minor | FAIL | Lines 37-50 are STILL FLAT 14 bullets. Changelog claim at line 84 is a self-contradicting entry (Critical 3). |
| `rev6 (if needed)` placeholder removed | PASS | Line 89 instead retains the same pattern for rev7 (Minor). |
| T3.4 commit message trimmed | PASS | Line 1524: `Commit: feat(iac): doReplace populates ApplyResult.ReplaceIDMap` (~62 chars including `Commit: ` label). |

---

**Options the author may not have considered:**

1. **Add a top-level `## Open Questions` section** immediately after `## Scope Manifest` listing the ProviderPlanner-deferral question + any other open items. Cross-reference from the ADR + W-9 intro. The user MUST encounter the ratification ask at the top of the plan. Closes Critical 1.

2. **Sweep all 3 stale `decisions/0001-...` references** (W-9 intro line 2328, TP1 line 2507, Final-Verification line 2710) to point at `docs/adr/006-...` AND fix line 2507's TP1 instruction to teach implementers the repo-native format. Run `grep -n "decisions/" docs/plans/...` after the sweep to verify only changelog/historical references remain. Closes Critical 2.

3. **EITHER apply the rev4 changelog grouping** (sort lines 37-50 into Critical/Important/Minor) OR remove the changelog claim at line 84. Don't ship a self-contradicting entry. Closes Critical 3.

4. **Sweep the 3 capitalized `PreservedFingerprint` residuals** in prose (line 295), commit message (line 414), and §3 prose (line 614) to lowercase. The commit message in particular is high-leverage — implementer copies it verbatim. Closes Important on incomplete sentinel-unexport sweep.

5. **De-couple the ComputeDrift test from the literal `"(unset)"` placeholder** by either introducing a package constant `unsetFingerprintPlaceholder` and referencing it from both ComputeDrift and the test, or by testing behavior (`PlanFingerprint != ApplyFingerprint`) instead of literal-string equality. Closes Important on test brittleness.

6. **Extend the W-3a rebase strategy paragraph** to specify WHO performs the rebase (implementer per receiving-code-review skill) AND name the W-1 prerequisite (W-1 must merge before T3.0.4 can rebase on its tip — both PRs touch `interfaces/iac_state.go`). Closes Important on hidden serial dependency.

7. **Verify or document the ADR-status-scanning tooling assumption.** EITHER grep the workflow repo for `Status: Accepted` or `Status:.*Accepted` and confirm no docs-validation script breaks on `Provisional`, OR change the Status to `Accepted (autonomous-pipeline; user-ratification deferred — see plan § Open Questions)` to preserve tooling compatibility while keeping honest provenance in the body. Closes Important on novel ADR status.

8. **Drop the `rev7 (if cycle-6 review surfaces findings)` placeholder** at line 89, matching the rev6 fix for the rev6 placeholder. Same pre-emptive-ghost-writing class. Closes Minor.

9. **Reconcile the T9.1 commit-message form** between the rev6 changelog (`ci(iac): cross-plugin build gate + ADR for ProviderPlanner deferral`, no "006") and T9.1's body (`ci(iac): cross-plugin build gate + ADR 006 for ProviderPlanner deferral`, includes "006"). One source of truth. Closes Important on internal inconsistency.

10. **Add a "Step 4 triage path" note to T3.0.4** describing how to isolate per-field test failures when 3 fields ship atomically. Acknowledges the 3-fields-in-one-task trade-off. Closes Important on T3.0.4 over-decomposition consideration.

---

**Verdict reasoning:**

Three Critical findings: (1) the rev6 ADR's `Status: Provisional — awaiting user ratification` cross-references `plan §W-9 § "Pending user ratification"` — a section that does not exist in the plan; the plan also has no top-level `## Open Questions` section, so the ratification ask has no operational surface for the user to encounter; rev6 nominally addresses cycle-5 Critical 2 (ghost-written user consent) but actually leaves the ADR in honest-attribution limbo with no path to becoming a real decision; (2) three stale `decisions/0001-...` references survive in active task bodies (W-9 intro line 2328, TP1 line 2507, Final-Verification line 2710) — the rev6 sweep updated only the rev6 changelog and T9.1 itself; line 2507 is particularly bad because it instructs IMPLEMENTERS to file FUTURE ADRs at the wrong path, re-introducing cycle-5 Critical 1 in the codemod ADR; (3) rev6 changelog line 84 claims `rev4 changelog block converted to grouped Critical/Important/Minor format` but lines 37-50 remain a flat 14-bullet list — self-contradicting changelog entry. Plus six Important findings (3 PreservedFingerprint capitalized residuals including a high-leverage commit message; T1.5 ComputeDrift test asserts literal `"(unset)"` placeholder; T9.1 commit-message form mismatch between changelog and body; W-3a rebase strategy silent on WHO + W-1 prerequisite; ADR Status `Provisional` novel vs. repo-precedent `Accepted`; T3.0.4 3-fields-in-one-task trade-off undocumented). Plus four Minor findings (rev7 placeholder retained; rev4 changelog wording; plan Status line uses bold-prefix; rev6 cache-path inline-comment fix verified PASS).

Per the prompt's PASS criteria (zero Critical AND every Important addressed): **FAIL**. The rev6 changes correctly addressed cycle-5's Critical 1 (ADR location/format) for the ADR file itself + addressed Critical 2 (`Decided by` ghost-attribution) by switching to honest provenance + addressed Critical 3 (T1.1 runtime pre-step) by removing it + addressed multiple Important findings (3-field consolidation, sentinel unexport, sentinel test, T9.1 commit boundary, W-3a rebase strategy, smoke-gate cost). But the rev6 changes introduced THREE new defects directly attributable to the rev6 changes themselves: (a) the ADR honest-provenance correction created a dangling cross-reference to a non-existent section, with no operational surface for user ratification; (b) the `decisions/0001-...` → `docs/adr/006-...` sweep was incomplete — same bug-class that cycle-5 caught for the ADR itself, but applied to the cross-references; (c) the rev6 author wrote a changelog entry claiming a fix that was not applied to the rev4 block — the kind of self-contradicting entry that erodes trust in the entire changelog.

Recommended next step (rev7): apply Options 1, 2, 3 (the three Critical fixes) atomically; apply Options 4-9 to close the Important findings. Specifically: (1) add `## Open Questions` section after Scope Manifest with the ProviderPlanner-deferral ratification ask; (2) sweep all 3 stale `decisions/0001-...` references to `docs/adr/006-...`; (3) actually apply the rev4 changelog grouping (sort 14 bullets into 3 sections) OR remove the changelog claim. Pattern observation across cycles 1-6: each rev correctly addresses the surface defects from the previous review but introduces 1-3 new defects of similar bug-class — repo-precedent conflicts, dangling cross-references, incomplete sweeps, self-contradicting changelog entries. The sweep-completeness defect is now THREE cycles deep (cycle-3 sentinel rename, cycle-5 ADR path, cycle-6 ADR cross-references); a rev7 should consider adopting a "sweep verification" checklist: after every rename/relocate, run `grep -n "<old form>"` over the file and resolve every hit OR explicitly note as historical.
