### Adversarial Review Report

**Phase:** plan
**Cycle:** 4 (unbounded per user direction)
**Artifact:** `docs/plans/2026-05-03-iac-conformance-and-replace.md` (commit 7cdac09)
**Status:** FAIL

---

**Findings (Critical):**

- **[Compile-error in plan pseudo-code / Hidden coupling between two functions]** §T3.0.5 Step 3 (line 1099) calls `snapshotEnvProvider(keys(plan.InputSnapshot), envProviderTolerantOfUnset)` — but `envProviderTolerantOfUnset` (line 1113) is a CLOSURE FACTORY whose signature is `func(plan map[string]string) func(name string) (string, bool)`. The call passes the function value itself (NOT the closure result), so the type mismatch is `func(map[string]string) func(string)(string,bool)` vs the expected `func(string)(string,bool)`. The pseudo-code does not compile. The fix is `envProviderTolerantOfUnset(plan.InputSnapshot)` — actually invoke the factory. Worse, the comment chain encodes a hidden contract: `envProviderTolerantOfUnset` returns `("", false)` when an env-var was set at plan-time but is now unset, with the comment "ComputeDrift handles this as preserved at plan-time fingerprint" (line 1122). But T1.5 Step 3's `ComputeDrift` (line 537) is a generic snapshot-vs-snapshot diff; it has no special-case handling for "key absent from applySnap because tolerated" vs "key absent because never set." If the closure returns `("", false)`, `inputsnapshot.Compute` (T1.2) drops the key from the applySnap — and `ComputeDrift` iterating over `planSnap` keys will see the key missing in applySnap and produce a DriftEntry. The "tolerance" the closure claims to provide is silently NOT honored by ComputeDrift. The TDD test `TestApply_Postcondition_FingerprintAfterEnvUnset_NoFalsePositive` (line 1066) WILL FAIL when implemented exactly as written. **Recommendation:** either (a) add a third "preserved" sentinel value to `inputsnapshot.Compute` + `ComputeDrift` and document the cross-function contract explicitly in T1.5 Step 3 (the formatter, sentinel, ComputeDrift, AND the W-3a closure all agree on the sentinel), or (b) drop the `envProviderTolerantOfUnset` factory and instead make ComputeDrift skip keys whose applySnap value is the canonical "unset" sentinel. Either way, T1.5's ComputeDrift signature/spec MUST mention the tolerance contract — the cross-PR coupling is silent in rev4.

- **[Internal contradiction between rev4 changelog claim and actual task content]** rev4 changelog line 41 explicitly states: *"Field-add sites corrected: T1.1 + T3.0.5 + T3.4 each list `Modify: interfaces/iac_state.go` for new ApplyResult fields"*. Verified against actual task content: **only T1.1 lists `Modify: interfaces/iac_state.go`**. T3.0.5's Files section (line 1027-1031) lists `iac/wfctlhelpers/apply.go`, `cmd/wfctl/infra_apply.go`, and two test files — NO `interfaces/iac_state.go`. T3.4's Files section (line 1238) lists ONLY `iac/wfctlhelpers/apply.go`. The rev4 fix INTENT is correct (consolidate field additions in T1.1 alone — that's the right Go-package-boundary answer); but the changelog claim is the OPPOSITE of the actual task content. An implementer reading the changelog and assuming T3.0.5/T3.4 add the fields themselves will produce a wrong-shaped commit set; a reviewer checking the changelog against the tasks will flag the contradiction. **Recommendation:** rewrite line 41 to read: *"All ApplyResult field additions consolidated in T1.1's `interfaces/iac_state.go` modification. T3.0.5 and T3.4 consume the already-declared fields without re-modifying the interfaces package."* Same for line 38 ("rev4 ships only ONE Modify of interfaces/iac_state.go, in T1.1").

- **[T3.0.5 numbering convention violates expectation set by other dot-suffixed tasks]** rev4 introduces T3.0.5 between T3.0 and T3.1, but in actual sequencing within W-3a (per the explicit "Sequencing within W-3a" note at line 1033) T3.0.5 ships AFTER T3.1. The dot-suffix precedent in this plan (TC1.5 at line 2317) was for an additive late-cycle task that lands after TC1 but before TC2 — the numbering matches the position. T3.0.5 places it between T3.0 and T3.1 numerically but its actual position is THIRD (T3.0 → T3.1 → T3.0.5). An implementer scanning the task headings will read T3.0.5 immediately after T3.0 and try to implement it before T3.1 — which won't compile because `wfctlhelpers.ApplyPlan` doesn't yet exist. The "Sequencing within W-3a" callout at line 1033 fixes this AFTER the fact, but the numbering itself misleads. The PR-Grouping table row (line 68) ALSO lists tasks in the misleading order: "T3.0, T3.1, **T3.0.5**, T3.2 – T3.5" — putting T3.0.5 in the visual position that contradicts its number. **Recommendation:** rename T3.0.5 → T3.1.5 (the task lands AFTER T3.1, before T3.2 — the dot-suffix-after-its-prefix convention is honored). Update the PR-Grouping table, the W-3a section header, all in-line cross-references, and the sequencing-within-W-3a note. Alternatively, add a new top-level task T3.10 (the W-3b runtime postcondition wiring task), but T3.10 is already used for the PR description task — so renaming to T3.1.5 is the cleanest path.

---

**Findings (Important):**

- **[Field-name capitalization drift across rev4 changelog vs task body]** §rev4 changelog line 7 says *"`upsertSupporter` hook + `replaceIDMap`"* (lowercase). Line 41 says `replaceIDMap` (lowercase). T1.1 declares `ReplaceIDMap` (capital R, exported) at line 188. T3.4's Rollback note (line 1282) uses `replaceIDMap` (lowercase). T3.4's commit message (line 1280) uses `ReplaceIDMap`. T1.1's commit message (line 238) uses `replaceIDMap` (lowercase). **An implementer reading the lowercase form will write `result.replaceIDMap` and the Go compiler will reject it (unexported field)**. The repo precedent (and Go convention) is to export struct fields used across packages. T5.1 (line 1712) parameter name is `replaceIDMap` (lowercase) — that's a parameter name, fine. But the `ApplyResult` field name MUST be `ReplaceIDMap` everywhere it's referenced. **Recommendation:** sweep + replace `replaceIDMap` → `ReplaceIDMap` everywhere in the plan EXCEPT inside T5.1's function parameter name (which is a local; lowercase is conventional). Also update rev4 changelog line 41 (which currently says "(`InitialInputSnapshot`, `InputDriftReport`, `replaceIDMap`)") to use the exported form.

- **[github-script JS pagination missing — sticky-comment dedup will fail on PRs with >30 comments]** §TP1 codemod-report.yml lines 2177-2181 use `await github.rest.issues.listComments({...})` without pagination args. GitHub REST API returns 30 per page by default; PRs with extensive review history (multi-cycle adversarial reviews, this very plan series being a prime example — already 4 cycles) will not return the marker comment in the first page. The script will fall through to `createComment` and produce a NEW comment per CI run. After enough runs, the PR comment thread is polluted with N copies of the codemod-summary. **Recommendation:** use `github.paginate(github.rest.issues.listComments, {...})` to fetch all pages, OR limit search to recent N pages (per_page: 100 + 3-page cap), OR add a fallback: search the most-recent 100 comments first, then check the second page only if no marker found. Pin the github-script action to a SHA per security policy precedent (e.g., `actions/github-script@v7.0.1` with the exact commit SHA — even first-party actions can move tags; the workflow security policy applies uniformly).

- **[T7.13 cache step ordering — cache key references `steps.hour.outputs.value` but no `id: hour` step exists]** §T7.13 YAML lines 1894-1899 declare `key: budget-${{ github.event.pull_request.base.sha }}-${{ steps.hour.outputs.value }}` — but the next step computes `HOUR=$(date -u +%Y%m%d%H)` inside the curl-runner step's bash (line 1903), NOT as a step output. The cache step LOOKS UP `steps.hour.outputs.value` BEFORE the bash runs, so the cache key resolves to `budget-<sha>-` (empty hour suffix) — every run for the same PR-base-SHA collides on a single cache key, and TTL bucketing breaks. **Recommendation:** add a discrete first step `- id: hour` `run: echo "value=$(date -u +%Y%m%d%H)" >> $GITHUB_OUTPUT`, then reference `steps.hour.outputs.value` from the cache step. Alternatively, use `${{ github.run_id }}` (per-run unique) for keying — but that defeats TTL caching. Pick one and make the YAML internally consistent.

- **[T7.13 cache: cached file consumed by SUBSEQUENT step but cache only writes if NOT cache-hit]** §T7.13 YAML steps:
  1. `actions/cache@v4` (line 1896) — restores `/tmp/budget-result.json` on hit.
  2. `if: steps.budget-cache.outputs.cache-hit != 'true'` curl step (line 1901) — fetches if not hit.
  3. `run: SPEND=$(jq -r ...)` step (line 1907) — reads `/tmp/budget-result.json` unconditionally.
  
  This is correct ONLY if `actions/cache@v4` writes back at job-end automatically when `cache-hit: false` — which it does (post-step writes). But this is the ONE-FILE case where `actions/cache` works; the surrounding plan text doesn't call out the cache write semantics, leaving an implementer wondering "where's the upload-cache step?" **Recommendation:** add a brief inline comment to the YAML explaining `actions/cache` post-step write-back; document the cache-write happens automatically at job-end so the file is fresh on miss + persists for future hits.

- **[ApplyResult.InitialInputSnapshot + ReplaceIDMap ship in T1.1 (W-1) but production population doesn't land until W-3a/W-5; field is permanently zero-valued in the persisted plan.json across multiple PR cycles]** §T1.1 declares 3 new ApplyResult fields. `InitialInputSnapshot` is populated by T3.0.5 (W-3a, post-merge of W-1+W-2). `InputDriftReport` is populated by T3.0.5 (W-3a) AND T1.5 (W-1, persisted-plan path). `ReplaceIDMap` is populated by T3.4 (W-3a) but only consumed by W-5 (T5.2/T5.3). For ALL three fields, the operator running `wfctl infra apply -o /tmp/result.json` between W-1 merge and W-3a merge will see the fields present in the JSON output but always empty — confusing surface area. Worse for `InputDriftReport`: T1.5 in W-1 populates it via the persisted-plan path, but T3.0.5 in W-3a populates it via the in-process path; until W-3a, the in-process path emits no drift report at all (no postcondition). Operators reading the field absence as "no drift" will be misled. **Recommendation:** either (a) gate the field's JSON serialization on `omitempty` (already done for all three — verified in T1.1 lines 178-188), AND add an explicit doc note to T1.1 that operators must check the wfctl version against the W-3a-merged tag before relying on `InputDriftReport`/`ReplaceIDMap` populated values; OR (b) defer T1.1's ApplyResult-field additions to a later T-task that lands inside W-3a as the FIRST commit, alongside T3.0.5's wiring. Option (b) is cleaner — the field exists exactly where its populator exists. The current rev4 plan mixes "schema-only field addition early" with "populator added later" across 2 PR cycles, recreating a milder version of the cycle-3 ghost-stub pattern at the field-declaration layer.

- **[rev4 dropping W-9's ProviderPlanner interface contradicts user mandate "don't defer any fixes"]** §rev4 changelog line 40 + W-9 section (line 2071) drop ProviderPlanner per cycle-3 YAGNI finding. The user's explicit instruction was: *"Don't defer any fixes, refactor if you have to, let's build these fixes the right way."* Cycle-3's recommendation was to drop ProviderPlanner because "no concrete consumer existed in the plan series." But the user-mandate framing is: don't DEFER (i.e., don't punt to later); BUILD it correctly. Dropping the interface IS deferral — it pushes the design conversation to a future PR with a future adapter. The other interpretation is that ProviderPlanner is SPECULATIVE, not a "fix" — there's no broken behavior it addresses, only a hypothetical future. Argument for dropping (rev4's choice): the user said "build these fixes" referring to the 8 root-cause issues, not "build every speculative interface that might be useful for future plugins." YAGNI applies. Argument against dropping (cycle-4 second look): the user also said "core-dump is in no rush" — there IS time to define the interface correctly, and Tofu/Pulumi adapters are on the workspace roadmap (per design rev2's "Out of scope" item: "Removing IaCProvider.Plan() interface method (preserved for v1 plugins; consolidation deferred)" — so consolidation IS in scope, just not in this plan). **Recommendation:** add an explicit ADR (`decisions/<NNNN>-providerplanner-deferred-to-first-consumer.md`) recording the cycle-4 reasoning: "The user-mandate 'don't defer fixes' refers to the 8 root-cause issues, not to speculative future-interface scaffolding. ProviderPlanner ships when the first concrete consumer (Tofu/Pulumi adapter) arrives with its own design discussion. The conformance-suite + cross-plugin-build CI gate (W-9 T9.1) provide the regression net for any future interface evolution." This converts a soft "we removed it" into a load-bearing decision the next reviewer can challenge or accept.

- **[T7.14 dedup logic: BOTH-labels filter creates a NEW failure mode where helper-filed issues that are manually labeled with one label become un-deduppable]** §T7.14 line 1952 changes the dedup `gh issue list` to require BOTH `conformance-leak-incident` AND `auto-filed-leak`. Failure mode: a maintainer triaging a helper-filed issue removes the `auto-filed-leak` label (e.g., re-labeling for a postmortem swimlane); the helper now no longer sees the issue as a dedup target and files a NEW issue on the next leak. The intent (cycle-3 fix: don't append to operator-only-labeled investigations) is correct; the implementation creates a different bug (don't-touch-the-helper-label-or-it-breaks). **Recommendation:** add a one-line operator note in `docs/conformance-runbook.md` (created in T7.13) under § "Helper conventions": "Do not remove the `auto-filed-leak` label from helper-filed issues during triage — it's the dedup key. To stop dedup'ing onto an issue, close the issue (the helper queries `--state open`)." Optionally also: file the dedup label as `helper:auto-filed-leak` (namespaced) so its mechanical purpose is visually distinct from operator labels.

---

**Findings (Minor):**

- **[Documentation]** §rev4 changelog (line 37-50) is now 14 bullet points — at the threshold where a future reader skims rather than reads. Consider adding a one-line summary heading per cycle (e.g., "rev4 (Critical fixes / Important fixes / Minor fixes)") and grouping bullets accordingly. The current flat list buries the 3 Critical-class rev4 fixes (T1.5 split; phantom-field; field-add-site) among less-significant changes.

- **[Documentation]** §T1.5 cross-PR note line 555 says *"the in-process apply path's drift postcondition lives in T3.0.5 as the first task after T3.1 in W-3a"* — clean. But the T3.0.5 heading itself (line 1025) does NOT cross-reference back to T1.5, making the connection unidirectional. A reviewer reading T3.0.5 first won't see the W-1 → W-3a logical split. Add a one-line "See T1.5 (W-1) for the persisted-plan-path counterpart of this in-process postcondition" to T3.0.5's intro.

- **[Documentation]** §T3.6e Rollback note line 1530 says *"The `differ_replace_test.go` file (NEW in this commit) is deleted by the revert. The `fake_provider_test.go` file (created in T3.6a, used by both T3.6a's no-op test AND T3.6e's Replace test) PERSISTS across the revert."* — this clarifies the cycle-3 minor finding cleanly. But the wording "PERSISTS" reads as if Git magic preserves the file across a revert; really, T3.6a's commit added the file and T3.6e's commit didn't touch it, so reverting T3.6e leaves T3.6a's commit (and its files) intact. Re-word to: *"The `fake_provider_test.go` file was created in T3.6a; reverting T3.6e leaves T3.6a's commit intact, so the file remains in tree."* — same outcome, less Git-magical phrasing.

- **[Documentation]** §line 18 Scope Manifest says *"PR Count: 12"* + *"Tasks: 72"*. After rev4's W-9 simplification (drop T9.1 + T9.2, keep T9.3 renumbered as T9.1), task count should be 70 (rev3 was 72 + T1.5-split-into-2 = 73; minus W-9's two dropped tasks = 71). The "72 (counting T3.6a–f as 6 separate tasks…)" parenthetical mentions the T1.5 split but doesn't account for W-9's task drops. Recompute. The same applies to PR-Grouping row 10: rev4's W-9 has only 1 task (T9.1, formerly T9.3); the PR Grouping table correctly says "T9.1" — clean — but the Tasks count needs the corresponding decrement.

- **[Documentation]** §line 22 revision history bullet for rev1 says *"FAILED plan-phase adversarial review (3 Critical + 7 Important + 5 Minor)"* — verify counts match the actual rev1 review. Likewise rev3's bullet (line 23) is missing the count format. Inconsistent metadata; reader uses these counts to gauge severity trajectory across cycles.

- **[Documentation]** §T9.3 → T9.1 renumber (rev4) — the W-9 "Future-required-method note" at line 2131 still references "T9.1's gate will turn red" which is correct post-rename, but the TP1 cross-reference at line 1873 says *"DO smoke gate CI workflow"* — verify all old "T9.3" references in the file have been swept to "T9.1". `grep "T9.3" docs/plans/2026-05-03-iac-conformance-and-replace.md` should return zero matches if the rename is complete.

---

**Bug-class scan transcript:**

| Class | Result | Note |
|---|---|---|
| Unstated assumptions | Finding | `envProviderTolerantOfUnset` factory's tolerance contract not encoded in `ComputeDrift` (Critical 1); cache step's `steps.hour.outputs.value` reference assumes a step that doesn't exist (Important); ApplyResult.* field semantics (zero-value vs not-yet-populated) ambiguous between W-1 and W-3a/W-5 (Important). |
| Repo-precedent conflicts | Finding | Field name capitalization drift (`replaceIDMap` lowercase in 4 places vs `ReplaceIDMap` exported field) — implementer following lowercase form gets compiler errors (Important). |
| YAGNI violations | Possible regression | rev4 dropped W-9 ProviderPlanner per cycle-3 YAGNI but the user-mandate "don't defer fixes" creates ambiguity about whether the drop was correct; recommend ADR (Important). |
| Missing failure modes | Finding | github-script JS sticky-comment lacks pagination → duplicate comments on PRs with >30 comments (Important); T7.14 BOTH-labels dedup creates new failure mode where label removal breaks dedup (Important); T1.5/T3.0.5 panic-recover guard correctly added but the *actual* compile-time bug in `envProviderTolerantOfUnset` invocation (Critical 1) was missed by the rev4 author. |
| Security / privacy | Clean | TP1 sticky-comment moved to first-party `actions/github-script@v7` per cycle-3 fix; one residual concern is even @v7 should be SHA-pinned per workflow security policy (Important — covered in github-script finding). |
| Rollback story | Clean | Each task carries a Rollback note; T3.6e wording clarified per cycle-3 minor (though phrasing tweak suggested as Minor). |
| Simpler alternative not considered | Finding | T3.0.5 numbering should be T3.1.5 (Critical 3); ApplyResult field-add could be moved to W-3a as first commit so field + populator land together (Important). |
| User-intent drift | Finding | rev4's W-9 drop arguably contradicts "don't defer fixes" mandate; needs ADR to record the cycle-4 reasoning (Important). |
| Over-decomposition / under-decomposition | Clean | T1.5/T3.0.5 split is the right shape; W-3a/W-3b sub-task ordering documented per cycle-3 (Important finding addressed). |
| Verification-class mismatch | Finding | rev4 changelog claim ("T1.1 + T3.0.5 + T3.4 each list `Modify: interfaces/iac_state.go`") contradicts actual task content (Critical 2). |
| Hidden serial dependencies | Finding | T3.0.5 numbered between T3.0 and T3.1 but actually ships AFTER T3.1; sequencing-within-W-3a callout fixes after-the-fact but task numbering misleads at first read (Critical 3). |
| Missing rollback wiring | Clean | Per-task Rollbacks present. |

---

**Cross-checks against rev4-claimed fixes:**

1. **T1.5 split into T1.5 (W-1) + T3.0.5 (W-3a)** — VERIFIED clean (no ghost-stub of `wfctlhelpers.ApplyPlan` in W-1; T3.0.5 appears as proper section with Files/Steps/Rollback; PR-Grouping line 68 reflects T3.0.5 in W-3a). PASS for the structural split. **HOWEVER:** T3.0.5 numbering convention is misleading (Critical 3); T3.0.5 implementation contains a compile-time bug in `envProviderTolerantOfUnset` invocation (Critical 1).

2. **`plan.InputNames` references corrected** — VERIFIED clean (zero `InputNames` references in the rev4 file; all use `keys(plan.InputSnapshot)`). PASS.

3. **ApplyResult fields declared in T1.1** — VERIFIED clean for T1.1 itself (lines 178-200 add the 3 fields + DriftEntry type to `interfaces/iac_state.go`). HOWEVER: rev4 changelog line 41 INCORRECTLY claims T3.0.5 + T3.4 ALSO modify `interfaces/iac_state.go` — they don't (Critical 2 — internal contradiction).

4. **W-9 ProviderPlanner DROPPED entirely** — VERIFIED clean (W-9 section header reflects "single CI-only PR"; PR-Grouping row 10 lists only T9.1; "REMOVED TASKS" anchor sanitized). PASS for execution. Important finding raised on whether the drop honors the user's "don't defer fixes" mandate.

5. **TP1 sticky-comment switched to actions/github-script@v7** — VERIFIED YAML uses first-party action only. PASS for supply-chain. Important finding raised on missing pagination + missing SHA pin (even first-party).

6. **T7.14 dedup requires BOTH labels** — VERIFIED helper script reflects this (`gh issue list --label "$PRIMARY_LABEL" --label "$HELPER_LABEL"`). PASS for execution. Important finding raised on new failure mode from helper-label removal.

7. **T1.5/T3.0.5 deferred postcondition wraps recover()** — VERIFIED `recover()` present (lines 1090-1098); test `TestApply_Postcondition_PanicDoesNotCorruptResult` exists (line 1056); test `TestApply_Postcondition_FingerprintAfterEnvUnset_NoFalsePositive` exists (line 1066). PASS for test scaffolding. **HOWEVER:** the Fingerprint-after-env-unset test will FAIL because of the hidden coupling between `envProviderTolerantOfUnset` and `ComputeDrift` (Critical 1).

8. **TC1.5 budget cap $25/mo gains $15/mo soft-alert + operator signoff** — VERIFIED (line 1913 implements the soft-alert curl path; line 1923 records signoff in `docs/conformance-runbook.md` § "Budget approval"). PASS.

9. **T7.13 cache backend specified as `actions/cache@v4`** — VERIFIED (line 1896). PASS for action choice. Important finding raised on `steps.hour.outputs.value` reference to a step that doesn't exist + cache write semantics not called out.

10. **W-3b sub-task cherry-pick rule documented** — VERIFIED (line 1347-1350 explicit "Sequencing constraint within W-3b" callout + cherry-pick rule). PASS.

11. **T9.1 paths filter added** — VERIFIED (line 2086-2094). PASS.

---

**Options the author may not have considered:**

1. **Defer T1.1's `ApplyResult` field additions to a single commit at the top of W-3a, instead of W-1.** The rev4 fix put all field additions in T1.1 (W-1) per the cycle-3 package-boundary critique. But this creates the "field declared early, populated later" surface area concern (Important finding). Cleaner: declare the fields in the SAME commit that populates them. T1.1 stays focused on `IaCPlan` + `PlanAction` schema fields (which W-1 actually uses for the persisted-plan path). T3.0.5 (or a new T3.0.4) declares + populates `ApplyResult.InitialInputSnapshot` + `InputDriftReport`. T3.4 declares + populates `ApplyResult.ReplaceIDMap`. Each PR ships a self-contained, observable behavior; no "field exists but is permanently zero" window.

2. **Rename T3.0.5 → T3.1.5.** Restores the dot-suffix-after-its-prefix convention that TC1.5 set. T3.1.5 sits between T3.1 and T3.2 numerically AND ships there sequentially. Renumbering downstream tasks (T3.2 → T3.3 → ... → T3.10) is unnecessary; the dot-suffix is the precedent. This eliminates the "Sequencing within W-3a" callout cost.

3. **Add a `Preserved` sentinel value to `inputsnapshot.Compute` + special-case in `ComputeDrift`.** Resolves the `envProviderTolerantOfUnset` compile-time bug AND the hidden ComputeDrift coupling in one motion. Add `const PreservedSentinel = "__plan_time_preserved__"` (or similar — a value that won't collide with a real sha256-prefix). `envProviderTolerantOfUnset` returns `(PreservedSentinel, true)` when the var was set at plan time but is now unset. `ComputeDrift` skips drift detection on keys whose applySnap value is PreservedSentinel. Document the contract in a single doc-comment block visible from all three call sites.

4. **Pin `actions/github-script` to a commit SHA, even though it's first-party.** The workflow security policy (per workspace memory) requires SHA pinning for all actions, including first-party (action tags can be moved by GitHub admins; SHA pinning is the only immutability guarantee). Adds `actions/github-script@de42dd5...` (commit SHA) + Renovate config entry. One-line cost; closes the residual supply-chain hole.

5. **Add a pre-condition assertion to T1.1's Steps that explicitly verifies ApplyResult is in `interfaces/iac_state.go` (not in another file).** rev4 didn't add the verification step the cycle-3 critic recommended. Insert as Step 0 of T1.1: `grep -n "type ApplyResult" interfaces/iac_state.go` should return one match before proceeding. This makes the package-boundary assumption explicit; an implementer running the steps gets a clean failure if the pre-condition fails.

---

**Verdict reasoning:**

Three Critical findings: (1) `envProviderTolerantOfUnset` is invoked as a non-closure value when its signature is a closure factory — pseudo-code does not compile + the test `TestApply_Postcondition_FingerprintAfterEnvUnset_NoFalsePositive` will fail because of an unencoded contract between the closure and `ComputeDrift`; (2) rev4 changelog line 41's claim that "T1.1 + T3.0.5 + T3.4 each list `Modify: interfaces/iac_state.go`" is the OPPOSITE of the actual task-body content — only T1.1 lists it (the intent is correct, the changelog is misleading; an implementer following the changelog produces a wrong-shaped commit set); (3) T3.0.5 numbering convention places it between T3.0 and T3.1 numerically but it ships AFTER T3.1 sequentially — the PR-Grouping table reinforces the misleading order. Plus seven Important findings (field-name capitalization drift between exported `ReplaceIDMap` and lowercase `replaceIDMap`; github-script JS sticky-comment lacks pagination; T7.13 cache step references a non-existent step output; ApplyResult fields ship before populators creating zero-value surface area for multiple PR cycles; rev4 W-9 drop arguably contradicts user "don't defer fixes" mandate; T7.14 BOTH-labels dedup creates new failure mode on label removal; cache write-back semantics not documented). Per the prompt's PASS criteria (zero Critical AND every Important addressed): **FAIL**. The rev4 changes correctly addressed the cycle-3 surface (T1.5 split, phantom-field elimination, field-add-site consolidation, third-party action removed, dedup-helper hardened, panic-recover added, budget-soft-alert added) but introduced two NEW classes of defect: (a) the hidden cross-function contract between `envProviderTolerantOfUnset` + `ComputeDrift` is unencoded and will cause one TDD test to fail at implementation time; (b) the rev4 changelog itself contradicts the task bodies in a way that misleads implementers (the kind of error a busy reviewer skipping straight to the changelog would propagate). Recommended next step: apply Options 1+2+3 from the "Options the author may not have considered" section (move ApplyResult field additions into W-3a as a self-contained commit; rename T3.0.5 → T3.1.5; add `PreservedSentinel` + ComputeDrift special-case to make the tolerance contract explicit). Also: revise rev4 changelog line 41 to match actual task content (single-source-of-truth in T1.1); add ADR for the W-9 ProviderPlanner deferral; sweep `replaceIDMap` → `ReplaceIDMap` everywhere except parameter names; add pagination to github-script JS; fix T7.13 cache key step ordering.
