### Adversarial Review Report

**Phase:** plan
**Cycle:** 2 of 2
**Artifact:** `docs/plans/2026-05-03-iac-conformance-and-replace.md` (commit b29b297)
**Status:** FAIL

---

**Findings (Critical):**

- **[Verification-class mismatch / Hidden serial dependency / Self-contradiction with the rev2 stated principle] §T3.6a Step 4 ships `t.Skip("dispatch lands in T3.6e")` to main as a way to keep CI green across the W-3 internal sub-task sequence.** The rev2 commit message explicitly removes this exact pattern from W-2/W-4/W-5/W-6 (notes at lines 728, 1391, 1473, 1512 — *"W-7 owns BOTH the stub creation and the body … no ghost-stub Go file ships in W-2"*). The author's own justification for removing those stubs ("if W-7 slips or is reverted, those stubs ship as permanent skip-only tests with no test value") applies identically to T3.6e: if T3.6e slips, the `t.Skip` lines in `platform/differ_replace_test.go` ship to main as no-op coverage. The PR Grouping table (line 40) bundles T3.6a–T3.6f into a single PR `feat/iac-replace-action`, which is good — but Step 5 of T3.6a says *commit* with the skip lines in place; T3.6b/c/d each have their own *Commit* steps; T3.6e's Step 1 says *"remove the `t.Skip("dispatch lands in T3.6e")` lines"*. So the skip lines exist in 4 separate commits on the W-3 branch before T3.6e removes them. If a partial cherry-pick of W-3 happens (e.g., reviewer asks to defer T3.6e to a follow-up PR; or git-bisect reaches a mid-W-3 commit), the in-tree state IS a permanent skip. **Recommendation:** either (a) collapse T3.6a–T3.6e into one commit (six tasks → one TDD red→green sequence in one commit) so the skip never persists across commits, or (b) explicitly document that T3.6a–f MUST be a single squash-merged unit and forbid mid-sequence cherry-pick, or (c) write the failing test in T3.6a but DON'T commit it until T3.6e (TDD red lives in worktree, green-after-each-step is the commit gate). Pick one and write it into the plan.

- **[Hidden serial dependencies on shared file / Drafting claim contradicts file-modification overlap]** §"Drafting (rev2 — finding-resolved)" line 2002 claims *"W-4, W-5, W-6 can draft + review in parallel after W-3 merges, but each modifies different files (`interfaces/iac_provider.go` for W-4 vs. `cmd/wfctl/infra_apply.go` for W-5 vs. `cmd/wfctl/flags.go` for W-6)"*. But:
  1. **W-9 also modifies `interfaces/iac_provider.go`** (line 1724 — T9.1 *"Modify: `interfaces/iac_provider.go`"*). The Drafting subsection lists W-4 and W-9 as both depending only on "post-W-3", with W-9 explicitly named as drafted in parallel with W-8 at line 2004. If W-4 (T4.1 `ProviderValidator` interface) and W-9 (T9.1 `ProviderPlanner` interface) draft concurrently, both touch the same interface file → merge conflicts.
  2. **W-5's T5.4 modifies `cmd/wfctl/infra.go::runInfraPlan`** (line 1454), and **W-1's T1.3/T1.6 also modify `cmd/wfctl/infra.go::runInfraPlan`** (line 256, 451). Although W-5 lands after W-1, the Drafting subsection's "different files" claim isn't true at the function level — these are concurrent edits to the same function in the same file. Same applies to **W-2's T2.3 modifies `cmd/wfctl/infra_apply.go` runInfraApply**, **W-5's T5.2 wires JIT into `iac/wfctlhelpers/apply.go`** (which W-3's T3.1 owns), **W-6's T6.1 modifies `cmd/wfctl/infra_apply.go`** — the *file-level* claim hides the function-level overlap.
  3. **W-7's T7.1 self-tests use a fake provider; W-9's T9.1 ProviderPlanner type-assertion is checked in `wfctlhelpers.ApplyPlan` (line 1727)**, but the rev2 line 1727 says ComputePlan also delegates to ProviderPlanner — this contradicts W-3's T3.6e implementation (line 1219), which has no ProviderPlanner branch. The Drafting subsection's "W-8 + W-9 can draft + review in parallel after W-3 merges" claim hides that W-9 also needs to retroactively edit T3.6e or T3.7 to add the ProviderPlanner branch. **Recommendation:** rewrite the Drafting subsection to call out function-level overlaps explicitly (e.g., "W-1, W-5 both edit `runInfraPlan`; coordinate by branching W-5 off W-1's tip"); split W-9's interface change into a "stage 0 of W-9" that lands with W-3 alongside T3.0/T3.7, OR add a T3.7.5 task to W-3 that includes the ProviderPlanner type-check stub so W-9 only adds the type definition; resolve the W-4 / W-9 `interfaces/iac_provider.go` race by sequencing W-9 strictly after W-4 (current graph allows them concurrent post-W-3).

- **[Verification-class mismatch / Missing failure modes] §T1.5 in-process apply path Step 3b "If any wfctlhelpers.ApplyPlan sub-action returns an error that mentions an unresolved/empty/changed env-var, re-fingerprint at error time"** — this is a string-match-on-error-message gate for the design's MOTIVATING bug. The plan offers no list of which error strings constitute "mentions an unresolved/empty/changed env-var", no typed error to match against, and no test that demonstrates a real DO API "field empty" error is caught by the heuristic. Cloud SDK error formats are notoriously variable across versions (godo, aws-sdk-go-v2, etc.) — a substring match for "env" or "unresolved" will either over-fire (false positives on every gRPC `Unavailable` error containing "env" elsewhere) or silently miss the actual case (cloud-side rejection like `invalid value` doesn't say anything about env vars at all; it says "field X must match pattern Y"). Step 3b also says "(or unconditionally as a postcondition check on plans that referenced JIT inputs)" — that "or" is an unresolved design decision, not an implementation. The TDD test `TestApply_PlanStaleDiagnostic_NamesChangedKeys` only exercises the persisted-plan path (lines 387-405); the in-process path's heuristic gets no failing test demonstrating the design's motivating bug (the `STAGING_PG_PASSWORD` cross-step env-var leak in `wfctl infra apply` without `--plan`). **Recommendation:** (a) define a typed error `iac/inputsnapshot.ErrEnvVarChanged` that wfctlhelpers.ApplyPlan wraps when a sub-action's resolved env-var differs from the plan-time fingerprint, AND (b) commit to the unconditional postcondition check (re-fingerprint the InputSnapshot at end-of-apply and emit the diagnostic if any fingerprint differs, regardless of whether any sub-action errored), AND (c) add a TDD test `TestApply_InProcess_PlanStaleDiagnostic_NamesChangedKeys` that exercises the in-process path with a STAGING_PG_PASSWORD-style fixture matching the design's motivating bug.

---

**Findings (Important):**

- **[YAGNI / Backwards-incompatible operator-facing change] §T3.6b adds a `--no-provider` escape hatch to `wfctl infra plan`** for "offline plan validation that skips Diff dispatch and falls back to ConfigHash-only behavior" (line 1154). The plan offers no concrete operator workflow that needs this: today `wfctl infra plan` doesn't load the gRPC plugin at plan time (the plan path skips provider load per review #1's verification of `cmd/wfctl/infra.go:281,327,375`), but post-W-3 it WILL load the plugin. The escape hatch flag preserves the prior offline behavior — but no documented use case justifies it (the Drafting subsection doesn't enumerate offline-plan operators; the design doesn't either). The flag adds a permanent CLI surface for a transient backward-compat need, and once shipped it can't be removed without a deprecation cycle. Worse: `--no-provider` plans would silently skip Replace classification (since Diff isn't called), so an operator using `--no-provider` against a v2 plugin gets a misleading plan output that doesn't reflect what apply will do. **Recommendation:** drop `--no-provider`. If gRPC-plan-load fails (network error, plugin not built), fail loudly with a clear message; don't fall back to a degraded-correctness path. If a real offline-plan workflow exists, document it and decide whether the v2 path is even appropriate (probably not — operators should use `wfctl validate` for offline).

- **[Repo-precedent conflicts / Missing failure modes] §TP1 codemod-report-in-PR-body** is novel for this codebase. The plan justifies the rationale at line 1794 by citing recording-decisions/SKILL.md, but no precedent exists in workflow-plugin-* repos for this pattern. Concrete concerns: (a) PR descriptions are limited (~65k chars on GitHub); the rev1 review noted potentially "hundreds of findings inline" — DO has 13 resource drivers each with potentially multi-finding shape; if the report exceeds the PR body limit, what's the fallback? (b) PR descriptions are mutable; if a maintainer edits the description later, the report is overwritten with no audit trail. (c) No per-finding link-back to the codemod source — readers can't easily reproduce the report locally. (d) `gh pr create --body-file /tmp/codemod-report.md` is shell-fragile (rev2 doesn't specify the embedding mechanism — manual paste, `gh` flag, or HEREDOC?). **Recommendation:** specify the literal embedding mechanism in TP1 (`gh pr create --body-file /tmp/codemod-report.md` with the report between `<details>` tags), specify a length cap with truncation behavior ("if report > 50k chars, attach as `gh pr comment --body-file` for the overflow + reference from the PR body"), and add a CI check that posts the codemod report as a `gh pr comment` artifact for durability. OR retreat to the simpler precedent-matching path: file the codemod report as a CI artifact (the workflow already uses GitHub Actions; artifact retention is 90 days by default).

- **[Missing failure modes] §T7.14 leak scrubber issue-filing has no rate-limit or dedup specified.** Line 1612: *"Hourly job: list resources tagged `conformance-pr-*` older than 1h; delete. Post a GitHub issue if anything was scrubbed."* With dozens of PRs racing, a single panicking-test failure mode (e.g., DO API rate-limit hits during cleanup) can spawn hundreds of identical issues per day. The `consecutive scrub events > 3/day` filter only applies to the *budget incident* path, not the *something-was-scrubbed* path. **Recommendation:** dedup by check-and-update an existing open issue (label `conformance-leak-incident`), only opening a new issue if no open one exists; add a once-per-day cap on issue creation; include scrubbed-resource counts in the issue body so subsequent hours append a comment rather than creating a new issue.

- **[Security / privacy / Unstated assumptions] §TC1.5 ephemeral DO project + §T7.13 conformance account creds.** TC1.5 line 1906 says *"in an ephemeral DO project named `coredump-cascade-dryrun-${date}`"* — but does NOT name the DO account that owns the project. T7.13 names `wfctl-conformance@gocodealone.dev` for the smoke gate creds. If TC1.5 runs against the conformance account, the dry-run cost is NOT included in T7.13's $5/month cap (the budget check pre-step only runs as part of the conformance smoke job, not part of TC1.5 which is a manual operator step in the C-1 PR). If TC1.5 runs against a personal account (Jon's DO account), that's an undisclosed personal-spend dependency on a project plan. The dry-run materializes 4 real resources (VPC, Volume, Droplet, Firewall) — even if torn down within minutes, it's not zero-cost. **Recommendation:** specify TC1.5 runs against `wfctl-conformance@gocodealone.dev`; bump the conformance budget cap (currently $5/month) to accommodate one ad-hoc cascade dry-run per major release cycle (~$1 of resources held for ~30 min); add a TC1.5 pre-step that calls the same `conformance-budget-check.yml` workflow as T7.13.

- **[Repo-precedent conflicts / Verification-class mismatch] §T9.3 cross-plugin build test runs `go mod edit -replace github.com/GoCodeAlone/workflow=../workflow`.** Line 1758. On a workflow PR, the workflow's CI is checked out at the PR head, NOT at main. The check therefore tests "does AWS/GCP/Azure compile against THIS PR's workflow" — which is the right test for a workflow PR. But the commit message *"ci(iac): cross-plugin build gate verifies AWS/GCP/Azure compile against workflow main"* (line 1770) claims it tests against main. This is a verification-class mismatch: the gate's value is testing against the PR head (catches breakage at PR time); the commit message advertises the wrong thing. **Recommendation:** fix the commit message to say "against this PR's workflow head" and document explicitly in the workflow YAML comment that the replace directive points to the PR's checkout. Also: T9.3 only runs `go build ./...`; it doesn't run `go test ./...`. If W-3's `IaCProvider` interface change adds a method (does it? T4.1 adds optional `ProviderValidator` separately, T9.1 adds optional `ProviderPlanner` — both optional via type-assertion, so non-implementers compile clean), build-only is sufficient. But the plan doesn't explicitly verify that NO required method was added — the cross-plugin build test would catch it if so, but the assertion is implicit. **Recommendation:** add a one-line note to T9.3: "if any future W-* PR adds a REQUIRED method to IaCProvider, this gate becomes blocking — workflow CI must call this out as a release-note."

- **[Missing failure modes] §T7.13 pre-flight balance check overhead.** Each smoke job runs `conformance-budget-check.yml` as a pre-step (line 1594). With dozens of workflow / DO-plugin PRs/day each touching `iac/`/`platform/`, the budget check fires dozens of times against the DO billing API. DO's billing API rate-limit is shared with the main DO API rate-limit (5000/hour for the conformance account); each pre-step makes 2 calls (`balance` + `billing_history`). This is fine at current PR volume but could become a contention point if the trigger pattern broadens. The $5 threshold is a guess (no historical data; not calibrated against actual smoke gate spend). **Recommendation:** cache the budget-check result per-PR-base-SHA (one check per PR series, not per-push) using GitHub Actions concurrency groups; calibrate the $5 threshold after 30 days of actual smoke-gate runs; document the calibration plan in `docs/conformance-runbook.md` (line 1591).

- **[Over-decomposition / Missing rollback wiring] §W-3 PR is now ~16 tasks (T3.0 + T3.1–T3.5 + T3.6a–T3.6f + T3.7 + T3.9 + T3.10).** Per writing-plans's "1-PR-1-feature" guidance, this is large for a single review. It bundles: a manifest schema (T3.0), a new package (T3.1–T3.4), a new cache subsystem (T3.5), a 6-task signature refactor (T3.6a–f), the manifest-driven dispatch (T3.7), runtime-launch-validation (T3.9), and PR description text (T3.10). The reviewer reads ~3000 lines of plan to evaluate ONE PR. The Critical finding above (T3.6a Step 4 skip pattern) is partly a symptom of this size — if T3.6a–f were their own PR, the skip-then-unskip cycle would be self-contained and reviewable. **Recommendation:** split W-3 into W-3a (foundation: T3.0, T3.1–T3.5; the manifest field + the new helper package + the cache, none of which change runtime behavior) and W-3b (refactor + dispatch: T3.6a–T3.6f + T3.7 + T3.9 + T3.10; the runtime-affecting changes). W-3a is reviewable in ~1500 lines + lands without runtime risk; W-3b is the binding behavior-change gate. The dependency graph already gates W-4/W-5/W-6 on "W-3 merged", so splitting doesn't change downstream order.

---

**Findings (Minor):**

- **[Repo-precedent conflicts] §Task Conventions line 59** still says *"Tasks before W-7 stub the assertion as `t.Skip("scenario added in W-7")` and W-7 fills the body."* This contradicts the rev2 stub-conformance removal: W-7 owns BOTH stub creation and body, so no task before W-7 should stub anything. **Recommendation:** rewrite line 59 to say "Conformance scenarios are referenced by name in earlier PRs' verification sections; the actual scenario stub + body both ship in W-7."

- **[Unstated assumption] §T3.5 `WFCTL_DIFFCACHE` env var values.** Line 1090 says *"set `WFCTL_DIFFCACHE=:memory:` for in-memory only (CI default — see #4)"*; line 1093 says *"CI workflows MAY set `WFCTL_DIFFCACHE=:memory:` to avoid filesystem writes in containerized runners (default off to avoid surprising operators)."* "CI default" vs "MAY set" is contradictory. **Recommendation:** pick one — either "CI workflows in this repo set `WFCTL_DIFFCACHE=:memory:` by default in `.github/workflows/test.yml`" (and add the env var to the workflow) or "CI workflows are unaffected; the cache writes to `$GITHUB_WORKSPACE/.cache/...` and is wiped on runner end".

- **[Verification-class mismatch] §T2.7 expected error string** "no provider configured for env staging" or "DIGITALOCEAN_TOKEN" (line 748) — actually two different literal expectations OR'd. Per writing-plans: *"per-task `Expected:` fields must be literal values the check can assert against"*. **Recommendation:** pick one literal error string the implementation will produce; document in T2.2 / T2.3 which path raises it.

- **[YAGNI] §T9.1 ProviderPlanner is checked "in wfctlhelpers.ApplyPlan (and ComputePlan)".** Line 1727. But neither T3.6e (ComputePlan implementation) nor T3.1–T3.4 (wfctlhelpers.ApplyPlan implementation) include any ProviderPlanner type-assertion. T9.1's TDD test claims this works but the implementation isn't in W-3 and isn't called out as an addition to W-3. Either T9.1 retroactively adds the type-assertion to W-3 code (but W-3 has merged by then), OR the TDD test just shouldn't claim ComputePlan/ApplyPlan delegate to ProviderPlanner today. **Recommendation:** add a Step to T9.1 that explicitly modifies `iac/wfctlhelpers/apply.go` and `platform/differ.go` to add the type-assertion branches; OR define ProviderPlanner as an interface that something else (a future PR or a Tofu-style adapter) consumes, not core wfctl. The "preserves Tofu/Pulumi-style extension capability" framing suggests the second is true — in which case W-9 ships the interface definition + a concrete Tofu adapter is out of scope.

- **[Documentation] §W-9 PR Grouping table line 47** says `T9.1, T9.2, T9.3` — confirmed renumbered from prior T9.1-T9.4. Clean.

- **[Repo-precedent / Documentation] §T8.3/T8.4/T8.5 marker references.** T8.5 explicitly says *"Skip if marker `// wfctl:skip-iac-codemod` present"* (line 1686). T8.3 (line 1655–1663) and T8.4 (line 1665–1678) DON'T mention the marker at all — only T8.5's note says ALL 4 modes honor it. **Recommendation:** add a one-line "honors `// wfctl:skip-iac-codemod` marker" sentence to T8.3 and T8.4 task bodies so an implementer reading just T8.3 sees it.

- **[Documentation] §T2.2 verification commit** doesn't specify the `mdformat --check` step that T2.6/T6.4/T9.2 specify (line 740, 1514, 1735). T2.2 is the new subcommand task; whatever it adds to `docs/WFCTL.md` should also pass mdformat. **Recommendation:** add the mdformat step to T2.2's Step 4 if it touches docs (or split the docs change to T2.6 explicitly).

---

**Bug-class scan transcript:**

| Class | Result | Note |
|---|---|---|
| Unstated assumptions | Finding | T3.5 `WFCTL_DIFFCACHE` "CI default" vs "MAY set" contradiction; TC1.5 DO account ownership unspecified. |
| Repo-precedent conflicts | Finding | TP1 codemod-report-in-PR-body novel pattern with no length cap or fallback; T2.7 expected error string isn't literal; Task Conventions line 59 contradicts the stub-removal. |
| YAGNI violations | Finding | T3.6b `--no-provider` escape hatch; T9.1 claim that ComputePlan/ApplyPlan delegate to ProviderPlanner without W-3 implementation. |
| Missing failure modes | Finding | T1.5 in-process error-string heuristic; T7.14 issue-spam dedup; T7.13 budget-check rate-limit. |
| Security / privacy | Finding | TC1.5 ephemeral DO project doesn't name the owning account; conformance budget cap doesn't include ad-hoc dry-runs. |
| Rollback story | Clean | T3.0–T3.10 each have appropriate Rollback notes; T3.4's expanded note covers mid-replace. |
| Simpler alternative not considered | Finding | W-3's 16-task PR could split into W-3a + W-3b; T3.6a–e skip pattern could collapse into a single commit. |
| User-intent drift | Clean | Plan respects user override (Path A, DO-only). |
| Over-decomposition / under-decomposition | Finding | W-3 PR is now ~16 tasks — too large for single review per writing-plans 1-PR-1-feature; T3.6a's skip-pattern under-decomposes the test-then-implement TDD cycle. |
| Verification-class mismatch | Finding | T1.5 string-match heuristic; T9.3 commit message advertises wrong target ("main" vs "PR head"); T3.6a TDD-skip pattern leaves dead `t.Skip` lines in 4 commits. |
| Hidden serial dependencies | Finding | Drafting subsection still claims W-4 + W-9 can draft in parallel but both modify `interfaces/iac_provider.go`; same for `runInfraPlan` modifications across W-1/W-5; W-9's ProviderPlanner type-assertion needs retro-edit to W-3 code. |
| Missing rollback wiring | Clean | All runtime-affecting tasks have Rollback notes. |

---

**Cross-checks against prompt's required scrutiny points:**

1. **T3.0 ships in W-3 alongside T3.7.** Co-shipping is fine because nothing reads the field until P-DO opts in (line 806 Rollback note explicitly notes this). Clean.
2. **T3.6a Step 4 `t.Skip` pattern.** Self-contradiction with rev2's stub-removal stance — Critical finding raised.
3. **T3.6b `--no-provider` escape hatch.** YAGNI — Important finding raised.
4. **T3.6b backwards-incompatible operator change.** `--no-provider` only partially preserves prior behavior (skips Diff dispatch but loses Replace classification). Acknowledged in finding above.
5. **T9.3 cross-plugin build test direction.** Tests against PR head, not main — commit message lies. Important finding raised.
6. **T7.14 leak scrubber issue spam.** No dedup specified — Important finding raised.
7. **TC1.5 dry-run account ownership.** Unspecified — Important finding raised.
8. **T7.13 pre-flight budget check overhead.** Per-PR API call without caching; $5 threshold uncalibrated — Important finding raised.
9. **T1.5 in-process apply path string-match.** Fragile heuristic — Critical finding raised.
10. **TP1 codemod-report-in-PR-body precedent.** No precedent in workflow-plugin-* repos; length/durability/embedding-mechanism unspecified — Important finding raised.
11. **W-3 task count.** ~16 tasks too large for one PR — Important finding raised (split recommendation).
12. **Drafting subsection vs file overlap.** W-4 + W-9 share `interfaces/iac_provider.go`; W-1 + W-5 share `runInfraPlan` — Critical finding raised.

---

**Options the author may not have considered:**

1. **Collapse T3.6a–T3.6e into a single TDD commit.** Each sub-task in T3.6a–e currently has its own commit with the skip lines persisting across 4 commits before T3.6e removes them. Pure-TDD discipline (red→green in one commit) would have all 6 sub-tasks ship as one commit on the W-3 branch: write the failing tests, implement signature change in differ.go, thread the provider through all 4 packages in one diff, implement Diff dispatch, wire cache. Larger single commit, but the cross-package signature change is genuinely atomic and should be reviewed as one diff anyway (a reviewer reading T3.6c can't evaluate it without T3.6a/b/d also visible).

2. **Sequence W-9 strictly after W-4 (not concurrent).** Current Drafting subsection allows W-9 + W-8 to draft in parallel after W-3. But W-9 (T9.1 modifies `interfaces/iac_provider.go`) and W-4 (T4.1 modifies `interfaces/iac_provider.go`) both touch the same interface. Sequencing W-9 after W-4 in the dep graph (`W-3 → W-4 → W-9`) eliminates the merge-conflict risk and costs nothing — W-9 is purely additive, W-4 is purely additive, ordering them is free.

3. **Replace T1.5's error-string heuristic with a typed `ApplyResult.InputDrift` postcondition.** Instead of pattern-matching error messages, have wfctlhelpers.ApplyPlan re-fingerprint env vars at end-of-apply unconditionally and write `ApplyResult.InputDriftReport []DriftEntry`. The diagnostic prints from this typed result, not from string-matching. Failure to apply is independent of drift detection — both can be reported. Removes the brittle heuristic + makes the design's motivating bug a structural test rather than a regex test.

4. **Defer T7.14 leak scrubber issue-filing entirely.** The scrubber DOES the cleanup (deletes orphans); the issue-filing is the alarm. Without the issue-filing, leaks still get cleaned up; with the issue-filing, the pager wakes up. If the alarm path is the brittle part, drop it for v1 and add it after observing actual leak frequency. The scrubber's value is bounded loss; the issue-spam concern goes away.

---

**Verdict reasoning:**

Three Critical findings (T3.6a's `t.Skip` pattern self-contradicting rev2's own stub-removal stance, hidden file-level serial dependencies in the rewritten Drafting subsection across W-4/W-9 and W-1/W-5, and T1.5's string-match-on-error-message heuristic for the design's MOTIVATING bug class) plus seven Important findings (`--no-provider` YAGNI escape hatch with degraded-correctness footgun, TP1 PR-body embedding mechanism unspecified, T7.14 issue spam without dedup, TC1.5 ephemeral DO project account/budget unspecified, T9.3 commit message lies about test target, T7.13 budget-check overhead/uncalibrated threshold, W-3 PR size at ~16 tasks too large for single review). Each Critical finding surfaces a concrete plan-execution failure mode the author can fix in revision. The Important findings are addressable but several would benefit from explicit acceptance in the open-questions section. Per the prompt's PASS criteria (zero Criticals AND every Important addressed): **FAIL**. This is cycle 2 of 2; per the skill bound, the next step is escalation to the user. The Critical-class findings are localized to specific tasks (T3.6a, the Drafting subsection, T1.5 Step 3b) and don't require structural rethink — but they DO contradict rev2's own stated principles, which suggests the author may have over-corrected on the stub-removal axis without noticing the pattern reappeared elsewhere.
