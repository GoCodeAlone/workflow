# External Plugin Example

Minimal external plugin that registers `step.uppercase` through the strict-proto
external plugin SDK.

Build it from the repository root through the example module:

```sh
cd example
GOWORK=off go build ./external-plugin
```
