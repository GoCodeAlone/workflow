# syntax=docker/dockerfile:1
# Generated by wfctl scaffold dockerfile --mode=generic
# Base: {{.BaseImage}}
# This Dockerfile is designed for use with `wfctl build`.
# It expects the workflow-server binary to be placed at the repo root.

FROM {{.BaseImage}} AS final

# Copy the workflow-server binary built by the CI pipeline.
COPY workflow-server /workflow-server

# Expose the HTTP port. Override via --expose-ports in app.yaml.
EXPOSE 8080

ENTRYPOINT ["/workflow-server"]
